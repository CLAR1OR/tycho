# CLAUDE.md — Tycho Roguelite

Working doc for AI agents (and humans) building **Tycho Roguelite**. **Read this first**, then the design bible: **`Tycho Roguelite.md`** (its top section, "Design Decisions — Locked", is the source of truth for what the game is).

---

## ⬇ Inbox — playtest feedback & quick to-dos
> The human drops notes here; agents triage them into the roadmap below. Newest on top. (Empty for now.)

-

---

## What this is (30-second version)
A 2.5D top-down **real-time action roguelite** (Hades-style *feel*) wrapped around a **rational-fiction story** and a **real-science tech tree**. Medieval setting; "magic" is secretly nanobots. The roguelite is the engine; **the story + learning is the soul.** Full vision + locked decisions: `Tycho Roguelite.md`.

## Current status (2026-07-08)
- **Mara's Forge — "The anvil" (F2) is built (2026-07-08, human picked it on claude.ai/design, "Forge Panel" group, with ONE amendment: NO stat bars — the BITE/PACE/REACH "character bars" from the mocks + the shared states card are explicitly rejected and built NOWHERE):** the forge is rebuilt from the old scrolling list into an **anvil screen** — one weapon at a time lying large on Mara's anvil in the ember light, the tabs switch it, refining is the single ceremony. `ForgePanel` (`src/town/forge_panel.gd`) is now a `Control` (was `PanelContainer`); the ember glow + tabs + anvil + big weapon + name bar + top-right Ore readout are drawn in a custom **`ForgeAnvil`** Control (`src/town/forge_anvil.gd`, `_draw` + tab hover/click hit-test, like `EtchingsArms`); each weapon's silhouette comes from **`WeaponSilhouette`** (`src/town/weapon_silhouette.gd`, static `paint()` shared by the tabs (small scale) + the anvil (large scale, slightly tilted, drop-shadowed); geometry lifted from the mock's SVGs as poly/line/arc primitives; unknown weapon id → `HudCore.monogram` fallback so a future weapon never crashes). **Components:** header (Cinzel `Mara's Forge` byte-identical + dim Garamond subtitle placeholder `She keeps working while you look.`); **Ore readout** top-right — a bare faceted-crystal glyph (`COL_ORE`) + big mono number of `resonance-ore` + dim `resonance ore` label, NO box (mirrors the etchings Dust readout the human set); **weapon tabs** (left column, one per weapon) — rounded slate box + small silhouette + tiny mono caps label (`SWORD · EQUIPPED` when equipped, else the name), hover brightens border→`COL_ORE` + silhouette, SELECTED wears gold border + gold label, click selects; **the anvil** centre stage — placeholder anvil silhouette (face+horn/waist/base) with the selected weapon on it LARGE + a **name bar** above (Cinzel ~30 name + mono caps meta `KIND · [EQUIPPED ·] FLAT Ln · +N% DAMAGE`, EQUIPPED gold only when equipped, `+N%` from `ForgePanelCore.damage_bonus_pct`); **the bottom strip** (slate PanelContainer, centred under the anvil) — the weapon's authored `desc` (Garamond, verbatim from `data/weapons/*.json`, NOT edited) + a mono level line (`REFINED Ln OF 5 · +15%/L` placeholder format) + the **refine pip track** (5 pips: filled = ore violet `COL_ORE` + glow / next = ore-violet outline / rest = slate outline; ALL-gold at max per the states card) + the action; **Close** bottom-left. **The action:** below max → the gold button `Refine to Ln (n Resonance Ore)` (byte-identical string, disabled when ore < cost, no red UI); at max → the byte-identical `Flat track maxed` as an inert gold-dim label; when the selected weapon is NOT equipped → also a slate `Equip` button (byte-identical, costs nothing); the equipped weapon shows no Equip button. **NO stat bars anywhere** (the amendment). **RESTYLE ONLY — mechanics byte-identical:** refine spends `resonance-ore` reason `forge-flat` from each weapon's `flat.costs`, equip writes `combat.current_weapon`, `WeaponCore`/`player.gd` untouched. **Pure `ForgePanelCore`** (`src/town/forge_panel_core.gd`, static, unit-tested): `refine_action(def, level)` → `{kind: refine|maxed, cost, to_level}` + `damage_bonus_pct(def, level)` → the name-bar `+N%`; both delegate to `WeaponCore` (`next_flat_cost`/`damage_mult`) — no cost/level math duplicated; the flat level is read at the call site with `WeaponCore.flat_level` and passed in as `level` (a signature nuance vs the brief's `weapon_state` — keeps the WeaponCore read as the single source, no duplication). **FROZEN public API** (the smoke drives it): `open/close/equip/upgrade` byte-semantics kept (the strip's Refine/Equip buttons bind straight to `upgrade`/`equip`) + a new `selected_weapon()` debug getter. `open_forge_panel()` gating in `town.gd` (B1), `PROCESS_MODE_ALWAYS`, group `forge_panel`, SlateTheme, pause-on-open, Sfx ui-click all UNTOUCHED. **SANCTIONED game-copy removals** (died with the list): `You carry: %d Resonance Ore` + the `%s%s  —  flat L%d (%.0f%% damage)` weapon-row string (that info is now the ore readout + the name-bar meta line). **No save-schema change; no data-file edit** (the weapon `desc`s render verbatim). Tests: `forge_panel_core_test.gd` (+4 — L0→refine cost 1, mid-track cost progression from real data, maxed at 5, bonus-pct math) → **182 test cases green** (was 178). Smoke: the forge block keeps passing (refine + equip daggers, daggers kit on the next run's player) + 2 new asserts (the anvil opens on the equipped weapon; a tab select switches which weapon is on the anvil) → **222 ok-checks green** (was 219–220; ±1 door-sigil nondeterminism stands). Editor pass clean. **HUMAN: everything is a PLACEHOLDER** — the ember glow colour/shape (`COL_EMBER`/`EMBER_*`), the anvil/weapon/tab geometry (`ForgeAnvil`/`WeaponSilhouette`, painterly pass), the ore crystal glyph, the pip sizes/radius, all visual consts at the tops of `forge_anvil.gd`/`forge_panel.gd`, and ALL new copy (`SUBTITLE`, the `LEVEL_LINE` format, the name-bar meta-line format — Mara's register, no aphorisms). The future **resonance-effects track** lands as a SECOND dust-cyan pip row in the strip when it exists. Shared palette/fonts live in `SlateHud` (spec: `design/ui-hud.md` → "Forge panel — The anvil (F2)"). The deferred "forge panel restyle" is now done.
- **The etchings panel — "The arms" (E1) is built (2026-07-08, human picked it on claude.ai/design, "Etchings Panel" group, with two amendments: no bottom hint chip; the Dust readout is a bare icon + big number, no box):** Thomas's meditation screen is rebuilt from the old scrolling list into Tycho's **two arms laid palm-up, four etched marks on them**. `EtchingsPanel` (`src/town/etchings_panel.gd`) is now a `Control` (was `PanelContainer`); the arms + marks + top-right Dust readout are drawn in a custom **`EtchingsArms`** Control (`src/town/etchings_arms.gd`, `_draw` + hover/click hit-test, like `TechChart`), and each mark's sigil comes from a small registry in **`SigilIcon`** (`src/town/sigil_icon.gd`, static `paint()` shared by the arms + the menu head; unknown ability id → `HudCore.monogram` fallback so an old save with e.g. Bolt equipped still renders). **THE NO-SWAP RULE (human's core directive):** the screen shows the four marks and NOTHING else — no ability list, no Equip button, no empty sockets, no dormant-four rows, no "slot" vocabulary in copy. A site shows its slot's **equipped-else-STARTER** ability (`EtchingsArmsCore.displayed_ability`; `STARTERS = {rmb:push, q:snare, r:shockwave}`, placeholder) + the fourth site is the innate **dash**. Bolt/Surge + the dormant four stay in data + `EtchingsCore` (untouched, learnable-in-principle), just unreachable here — swapping arrives later as a story beat. **Site → ability:** right palm = RMB (Push), left palm = Q (Snare), right forearm = R (Shockwave), left forearm = SPC (dash, hardcoded). **Mark states** (hover = mouse-motion hit-test, redraw on change; click = select → opens the menu): DORMANT faint/no core dot, AWAKE resting cyan glow, HOVERED full + halo + key badge, SELECTED gold tint + gold halo + dashed ring; dust-cyan `COL_DUST` = idle/hover, gold `COL_READY` = selection. **The skill menu** (right-side Slate dock, like the tech dock): sigil + name (Cinzel) + key chip; principle (dust) + `%ds cooldown` (dim) chips; description (Garamond, from data); the **`THE MARK DEEPENS`** level track (pip + blurb per level from `current`→MAX: now gold / next hollow / deeper dim `The resonance goes deeper.`); then the action button — dormant → **`Awaken (n Dust)`** (the picked reframe of the old `Learn`; same `EtchingsCore` unlock), L1/L2 → the byte-identical `Deepen to L%d (n Dust)`, L3 → inert `Mastered (L%d)`, can't-afford → disabled (no red UI). The **dash menu** is description + principle chip only — no track, no button ("It does not deepen here"). **AWAKEN AUTO-EQUIPS (the one real behavior change):** with no Equip button, awakening a mark equips it to its slot — `learn()` already auto-equips a fresh unlock into an empty slot, which is exactly this case. **Pure `EtchingsArmsCore`** (`src/town/etchings_arms_core.gd`, static, unit-tested): `displayed_ability` (equipped-else-starter) + `menu_action(def, etchings)` → `{kind: awaken|deepen|mastered, cost, to_level}` (layered on `EtchingsCore.learn_cost`, no cost math duplicated). **Player-facing copy is now data:** two OPTIONAL fields added to the `etchings` DataLoader spec — **`desc`** (menu description) + **`level_blurbs`** (per-level track lines) — authored in `push.json`/`snare.json`/`shockwave.json` ONLY (the other 6 unchanged; missing → empty desc / `L%d` fallback). **REMOVED game copy** (sanctioned, with the list): the section headers (`RMB — Strikes` etc.), `Equip`/`(equipped)` labels, and the dormant line `The resonance does not answer this one yet.` **FROZEN public API** (the smoke drives it): `open/close/learn/equip` byte-semantics kept (`learn` body unchanged; `equip` retained though the screen shows no Equip button — the smoke still exercises it) + new `open_menu(slot)` (arms callback) and `site_ability(slot)` debug getter. `open_etchings_panel()` gating in `town.gd`, `PROCESS_MODE_ALWAYS`, group `etchings_panel`, SlateTheme, pause-on-open, Sfx ui-click, and the `etching` Ledger reason all UNTOUCHED. **No save-schema change.** Tests: `etchings_arms_core_test.gd` (+5 — equipped-wins, starter-fallback, awaken/deepen/mastered actions) → **178 test cases green** (was 173). Smoke: the etchings block keeps passing with presentation-level adaptations + 5 new asserts (the four sites via `site_ability`, the dash site inert, awaken auto-equips Snare/Shockwave) → **220 ok-checks green** (was 214–215; ±1 door-sigil nondeterminism stands). Editor pass clean. **HUMAN: everything is a PLACEHOLDER** — the arm silhouettes + sigil geometry (`EtchingsArms`/`SigilIcon`, awaiting the painterly pass; the Dust glyph is a mote cluster), all visual consts (`HIT_R`/halo/badge/dust-number sizes + arm dimensions), the `STARTERS` map, and **ALL new copy** (`SUBTITLE`, `DORMANT_DESC`, `DASH_DESC`, `DASH_PRINCIPLE`, `DEEP_BLURB`, `MARK_DEEPENS`, and the `desc`/`level_blurbs` in the three JSONs) — dial/curate freely (spec: `design/ui-hud.md` → "Etchings panel — The arms (E1)"). The deferred "etchings panel restyle" is now done.
- **The research screen — the "Star chart" (R1) is built (2026-07-08, human picked it on claude.ai/design, project "Tycho UI", the "Research Screen" group):** Sophia's desk / tech tree is rebuilt as a **constellation map** (Tycho Brahe charted stars; Sophia charts what the resonance knows). The old LIST + NODE screens merge into a fullscreen **CHART**; the solve flow (READ → QUIZ/PUZZLE → LOCKED/AHA) keeps its scrolling reading page with two small Slate touches. `TechPanel` (`src/learning/tech_panel.gd`) is now a `Control` with two child roots toggled by screen: the **chart** (new **`TechChart`**, `src/learning/tech_chart.gd`, a custom-drawn Control like the HUDs — stars + prereq edges + progress arcs + starfield dust in `_draw`, click-to-select in `_gui_input`) with the header/dock/hint/close, and the **reading page** (the original margin→scroll→rows stack). **Components:** header (Cinzel title `Sophia's Desk — Research` byte-identical + a dim Garamond subtitle placeholder; top-right carry chip in the knowledge/shards colours + the shipped turn-in button); the **constellation** (each node a star at a `chart_pos`, prereq edges between them, dashed selection ring); a right-side **detail dock** (Slate PanelContainer — name, tier/age/kind/🧠 meta, progress bar + `n/cost`, `Requires:`/`Unlocks:` with building ids resolved to display names, then the shipped ready-line + `Read & solve` or `Invest everything you carry`); an **Age-II tease** (unnamed dim dots + right-edge fade + caps label); deterministic **starfield dust**; a **hint chip** + **Close**. **Pure `TechChartCore`** (`src/learning/tech_chart_core.gd`, static, unit-tested — layered on TechCore predicates) owns `node_state` (precedence **researched > locked > ready > active > available**; quiz-lock is ORTHOGONAL — a ready node's locked quiz shows peril-red meta, the ONLY red), `edge_kind` (**lit > dim > open**), and `chart_pos` (authored `[x,y]` 0..1 or a DETERMINISTIC in-band fallback from the id, so an unpositioned node never crashes/overlaps the dock; `has_chart_pos` for fallback warnings). **New data field:** optional `"chart_pos"` on the `tech` DataLoader spec, authored in both JSONs (arithmetic `[0.18,0.58]`, masonry `[0.34,0.46]` — placeholders, only the field added, node content untouched). **Colour lift:** `COL_STONE`/`COL_FOOD`/`COL_KNOWLEDGE` moved UP from `TownHud` to `SlateHud` (pure lift, same values, resolves in TownHud by inheritance) so the chart reads the knowledge colour off the ONE dial source. **Solve-flow touch:** the AHA title (`✦ <name>`) renders in gold `COL_READY`; everything else on READ/QUIZ/LOCKED/AHA byte-identical. **FROZEN public API** (the smoke drives it): `open/close/select_node/invest_all/turn_in_shards/begin_read/begin_quiz/answer/begin_puzzle/puzzle_node/finish/on_locked_screen` (select_node still sets active + now shows the dock; finish returns to CHART) + a new `star_state(id)` debug getter. All TechState routing, save calls, Sfx ui-click, quiz shuffle-index binding, the PUZZLES registry + `solved` signal, and wrong-answer→lock→LOCKED are byte-identical. **No save-schema change.** Tests: `tech_chart_core_test.gd` (+5 — state precedence, edge kinds, chart_pos passthrough + deterministic in-band fallback, real-data positions) → **173 test cases green** (was 168). Smoke: +4 chart asserts where the panel is already open (arithmetic `available` + masonry `locked` before research; both `researched` after the gateway) → **214 ok-checks green** (was 210–211; ±1 door-sigil nondeterminism stands). Editor pass clean. **HUMAN: every chart visual number is a PLACEHOLDER** (star radii per state, glow/arc/ring widths, dust positions/alpha, right-fade width, hit radius, dock width/bar height — top of `tech_chart.gd` + `tech_panel.gd`) plus the new UI copy (subtitle, carry/dock labels, hint, Age-II label, quiz-lock meta) and the two `chart_pos` values — dial like FEEL numbers; the shared palette/fonts live in `SlateHud` (spec: `design/ui-hud.md` → "Research screen — Star chart (R1)").
- **Panels + pause menu restyled to Slate; pause menu is now FULLSCREEN (2026-07-07, human directive: "continue with the pause/panel restyle to slate. pause menu should be fullscreen"):** the six Control-tree UIs that were plain default-gray now speak the Slate language via a shared Godot `Theme`. **New `SlateTheme`** (`src/core/slate_theme.gd`, `class_name SlateTheme extends RefCounted`) — a static factory + cache (`get_theme()` builds ONE `Theme`, memoised in a static var) that reads every colour + font **off `SlateHud`'s constants** (`COL_SLATE_BG`/`COL_SLATE_BORDER`/`COL_TEXT`/`COL_READY`/`COL_KEY_TEXT`/`COL_CHIP_BORDER` + the three fonts via `SlateHud._with_fallback`) so palette/fonts stay ONE dial source shared with the `_draw` HUDs; only sizes/margins/radii are `SlateTheme`'s own placeholder consts. It styles base **Button** (5 slate styleboxes: normal / hover→border lightens to `COL_TEXT` / pressed→bg darkened / focus→gold `COL_READY` border / disabled→dim), **Panel/PanelContainer** (slate `panel` box ~0.97 alpha), **Label** (`COL_TEXT`), plus four **type variations** applied per-node via `theme_type_variation`: `TitleLabel` (Cinzel caps titles), `NumLabel` (mono numeric readouts), `DimLabel` (Garamond dim key colour — hints/footnotes/statuses), `MenuButton` (Button in Cinzel, roomier margins). Each UI sets `theme = SlateTheme.get_theme()` on its root and swaps ad-hoc `add_theme_font_size_override`/`modulate` fiddling for the variations — the diffs got SIMPLER. **Six UIs:** `PauseMenu`, `ForgePanel`, `TechPanel`, `EtchingsPanel`, `DialoguePanel`, and **`EchoOfferPanel`** (the in-run pick-1-of-3 — a SCOPE ADDITION beyond the deferred list, included so no default-gray UI remains; noted to the human). **RESTYLE ONLY** — every public method, signal, flow, pause semantic, guard, `_button` ui-click Sfx wiring, and game-copy string is byte-identical; the smoke drives all six unchanged. **The pause menu became FULLSCREEN** (the one structural change): `PauseMenu` changed base `PanelContainer`→`Control` — a near-opaque backdrop (`#0c0b10` @ 0.88, a const) over the whole screen + a centred column (Cinzel `Paused`, the existing Resume/Forfeit/Quit buttons as `MenuButton`s min-width 320, the gate-refusal line as `DimLabel`). **CanvasLayer geometry quirk handled** (the RunHud bug — anchors set in a Control's own `_ready` under game.gd's `$HUD` CanvasLayer get no layout pass): `size` syncs to `get_viewport_rect().size` in `open()` AND each frame in `_process` while visible (still `PROCESS_MODE_ALWAYS`). All flow (tree pause/unpause, guards, inert-at-slot-select, the Hades gate) untouched. **Un-themed (out of scope):** slot-select, F1 tuning panel, F2 cheat panel, the arch puzzle (draws its own diagram). **No save-schema change, no new data files, no `# FEEL:`/audio/game-copy edits.** Tests: **168 test cases green** (unchanged — no pure logic emerged; no theme test added). Smoke: +1 assert (open the pause menu in town, assert its root spans the viewport — the geometry net — then close + restore) = 2 new ok-checks → **211 ok-checks green** (was 208–209; +2 mine, ±1 door-sigil nondeterminism stands). Editor pass clean. **HUMAN: `SlateTheme`'s sizes/margins/radii + the pause backdrop colour are PLACEHOLDERS** (dial like FEEL numbers); the shared palette/fonts live in `SlateHud` (spec: `design/ui-hud.md` → "Panels & the pause menu — Slate"). The deferred "pause / panel restyle to the slate language" item is now done.
- **The town HUD — "T1 slate strip + overnight toast + per-resource projections" (2026-07-07, human picked it on claude.ai/design, project "Tycho UI", the "Town HUD" group):** the town's plain `DayInfo`/`FoodStatus`/`Hint` Labels AND `game.gd`'s stacked `$HUD/Resources` readout are retired for a proper Slate HUD reusing the in-run HUD's exact visual language + fonts. **A shared base was extracted first: `SlateHud`** (`src/core/slate_hud.gd`, `class_name SlateHud extends Control`) — the palette + pickup/resource colours, the three project fonts (`_with_fallback`), the shared `FS_CHIP/BODY/HINT/SMALL` + `MARGIN`, the common `_ready` (FULL_RECT + `MOUSE_FILTER_IGNORE` + build the `FontVariation`s), the `_sync_viewport_size()` helper (the CanvasLayer-under-`_ready` layout quirk), and the draw plumbing (`_panel`/`_text_w`/`_text_in`). **`RunHud` is now `extends SlateHud`** — a pure zero-render lift (drops its duplicated palette/fonts/helpers, keeps the run-specific HP/echo/slot/boss/vignette/pickup consts + code; `_ready` calls `super._ready()`, `_process` starts with `_sync_viewport_size()`). New **`TownHud`** (`src/town/town_hud.gd`, `extends SlateHud`, `PROCESS_MODE_ALWAYS` so it survives town-entry cutscene pauses) drawn in `_draw`, created by `town.gd` onto its `$HUD`. **Four components:** (1) **day chip** top-left (`Day 4 · Well-Fed +25%` gold span / `Day 4 · Short on food` dim span / `Day 1` pre-tick — NEVER red; the +25% derives from `TownCore.WELL_FED_BONUS`); (2) **resource strip** top-right — seven columns gold/stone/food/knowledge/shards/ore/dust, value (per-resource colour, three new colours stone `#b5ada0`/food `#a4d97a`/knowledge `#9fdcff`) + dim label, polling the Ledger per frame — **split same-day (human request) into TWO panels** under tiny dim Cinzel `TOWN`/`RUN` headers (placeholder copy): the **town economy** (resources a building can generate on a day tick — derived from the building defs via new pure `TownHudCore.producible_resources` (`produce` effects + the `knowledge` kind), so a future gold-producing Market would migrate gold automatically; today stone/food/knowledge) and the **run pickups** (gold/shards/ore/dust = exactly the in-run pickup-strip set, holding the top-right corner in both scenes); the split added +2 unit cases +2 smoke checks → **168 test cases / 208–209 ok-checks**; (3) **projections** BELOW each column — a ~0.55-alpha `+n/d` net-per-day generation from `TownHudCore.day_deltas(TownCore.tick(…))`, food nets its upkeep, recomputed on `resource_changed`+`building_built` (not per frame); (4) **hint chip** bottom-center (placeholder copy). **Plus the overnight toast** top-center: `game.gd` stashes each day tick (`_last_day_tick`, in-memory) and hands it to `town.show_day_toast(tick)` on the run-end return — the **Forfeit path never sets it, so it never toasts** (Forfeit ticks no day). Slate panel, Cinzel `OVERNIGHT` header + a row of `TownHudCore.toast_segments` (`+5 gold` display-ordered, `-5 eaten` in the food colour when consumed, `Well-Fed` gold when fed); pickup-strip lifecycle (hold `TOAST_HOLD_S` 4s, fade `TOAST_FADE_S` 1s). **Pure `TownHudCore`** (`src/town/town_hud_core.gd`, static, unit-tested) owns `day_chip_text`/`day_deltas`/`projection_text`/`toast_segments`. **DELETED:** `town.tscn`'s DayInfo/FoodStatus/Hint Labels + their `town.gd` fills; `game.tscn`'s `$HUD/Resources` Label + `game.gd`'s `HUD_RESOURCES`/`_res_label`/`_refresh_resources()` + all call sites (incl. the resource_changed/save_loaded connects). **No save-schema change** (the toast dict is in-memory). Tests: `town_hud_core_test.gd` (+11) → **166 test cases green** (was 155). Smoke: TownHud found via a `town_hud` group on the victory return (toast visible, day chip `Day 2` + food status, spans the viewport — the CanvasLayer geometry net), and a stone-projection self-consistency check after the quarry is built → **206 ok-checks green** (was 200; ±1 door-sigil nondeterminism stands). Editor pass clean. **DEVIATION FROM SPEC:** the day-chip `+25%` literal is `str(int(TownCore.WELL_FED_BONUS * 100))` (WELL_FED_BONUS is 0.25, a bonus fraction), NOT the spec's `(WELL_FED_BONUS - 1.0) * 100` which assumed a 1.25 multiplier and would print `-75`. **HUMAN: `HINT_TEXT`, the toast copy, `TOAST_HOLD_S/FADE_S`, `PROJECTION_ALPHA`, the `FS_*`, and the three new resource colours are PLACEHOLDERS** (TownHud consts; shared palette/fonts in `SlateHud`) — dial like FEEL numbers (spec: `design/ui-hud.md` → "Town HUD"). The deferred list's "Town HUD restyle" item is now done.
- **HUD fonts + text centering (2026-07-07, human dropped font families in `temp/fonts/`):** the HUD's fallback-font text is replaced by **three OFL fonts** shipped in `assets/fonts/` (provenance + license copies: `assets/fonts/SOURCES.md`), one role each — **Cinzel SemiBold = display** (ability/echo monograms + the `FLOOR N — BOSS` label; Cinzel renders lowercase as small caps, so `Sn`/`Sh`/`Su` stay distinct), **JetBrains Mono Medium = every number/readout** (info chip, HP numerals, cooldown seconds, pickup strip, key/stack badges — mono digits don't shuffle as values tick, and it natively carries the chip's `⚠`), **EB Garamond Medium = the one prose line** (the contextual hint; Garamond runs small for its px so a new `FS_HINT` 19 sits above the old body size). Each font is wrapped in a `FontVariation` with `fallbacks=[ThemeDB.fallback_font]` (`RunHud._with_fallback`) so a missing glyph never renders as a box, without mutating the shared imported resource. **Held back** for the dialogue panel / codex / titles later: Cormorant, Cormorant Garamond, IM Fell English — the full family drops stay in `temp/`, now **`.gdignore`d + gitignored** (the first editor scan started importing all ~70 unused files). **The human-reported "text sits low in the boxes" bug is fixed:** the old `_text` helper sized panels by the font-size px and dropped text by the full ascent, but a line box is ascent+descent tall (~20px at fs 14 — measured), so text rode ~3px low everywhere; the replacement `_text_in(rect, …)` centres via baseline math (`centre + (ascent − descent)/2`) and panels size by `font.get_height(fs)` (the slot-face magic `-4.0` nudge died with it). Only `run_hud.gd` + `assets/fonts/` touched — `HudCore`, tests, and the smoke needed no changes. **HUMAN: the font files + per-role `FS_*` sizes are placeholders in the Style block — dial like the rest.** Tests **155 green** (unchanged), smoke **200 ok-checks** green, editor pass clean.
- **The real in-run HUD — the "Slate" design is built (2026-07-07, human picked it on claude.ai/design):** the debug-style stacked text Labels are retired for a proper HUD. The human ran a HUD design round on **claude.ai/design** (project "Tycho UI", the **HUD — C iterations** group) and picked the **Slate** visual language + the **C** direction (an echo shelf over the HP bar) married to a **C2-style clean top row**. New **`RunHud`** (`src/combat/run_hud.gd`, a screen-filling `Control`, `mouse_filter` IGNORE) added by `combat_room.gd` onto its `$HUD` CanvasLayer; it **draws everything itself in `_draw`** (rounded slate panels via a `StyleBoxFlat`, text via the fallback font) and polls the player/boss per frame, with `combat_room`/`game.gd` pushing room/hint/wave/HP/boss state through setters. **Seven components:** (1) top-left **info chip** `F2 · R3/5 ⚠ · Wave 2/3` (floor always; R/BOSS/Reprieve by kind; red ⚠ only on a peril-door room; `Wave n/m` only in a multi-wave room while uncleared — wave progress LEFT the hint); (2) top-right **fading pickup strip** (in-run gold/ore/dust/shards only, colored text no icons, snaps on `resource_changed` while `in_run`, holds ~3s then fades ~0.8s); (3) bottom-left **HP bar** with a **low-HP** state at ≤25% (red border + number tint + a cheap red screen-edge **vignette**); (4) an **echo shelf** of 32×32 monogram tiles directly above the HP bar (initials, e.g. Tempest Stride→`TS`; stackable repeats fold into one tile + a gold count badge; wraps after 8; rebuilt on spawn + each pick from `RunState.echoes`); (5) bottom-right **ability slots** RMB/Q/R (66×66) + a `SPC` dash pip (46×46) with per-slot monograms (`P`/`B`/`Sn`/`Sh`/`Su`), cooldown face-darken + seconds, gold "ready" border; (6) bottom-center **contextual hint** (exit/door/wellspring/artifact strings); (7) top-center **boss bar** (boss rooms only, tracks the boss node's hp, gone on death). **Pure `HudCore`** (`src/combat/hud_core.gd`, static, unit-tested) owns the segment rules (`chip_text`), the echo fold (`fold_echoes`), the monogram scheme (`monogram`), and the low-HP threshold (`is_low_hp`) — `RunHud` owns only pixels. **Clean getters, no poking privates:** `player.ability_slot_info()` (per-slot {id,name,cd_left,cd_total} + a `dash` entry) and `enemy_dummy.current_hp()` were added for the HUD to read. **Dash reality:** the player has no dash-cooldown accessor beyond `_dash_cd`, so the pip sweeps during that cooldown and is otherwise ready (no FEEL touched). **REMOVED:** `combat_room.tscn`'s `RoomInfo`/`HP`/`Hint` Labels + the code-built `_ability_label`; `game.tscn`'s `$HUD/Echoes` label + `game.gd._refresh_echoes`. `game.gd`'s `$HUD/Resources` town-economy readout now **hides while `RunState.in_run()`** (in-run pickups are the fading strip; it shows again in town). Reprieve/boss/artifact/door flows are unchanged — only their HUD text reroutes to `RunHud` setters. **No save-schema change.** Tests: `hud_core_test.gd` (+18 — chip segments, monogram edge cases, fold ordering/stacks, low-hp boundary) → **155 test cases green** (was 137). Smoke adapted (reads `RunHud` state instead of Label text) + **+5 new ok-checks** (info chip shows `Wave` in a multi-wave room; echo shelf tile count == folded pick count after a pick; pickup strip lit on a gold drop; boss bar visible in the boss room then gone after the kill) → **199 ok-checks green** (was 193–194; ±1 door-sigil nondeterminism stands). Editor pass clean. **Same-day fix (human playtest: only the top-left chip visible):** anchors set in a Control's own `_ready` under a CanvasLayer get NO layout pass (Godot quirk — `size` stayed 0,0 so every size-anchored element drew off-screen); `RunHud._process` now syncs `size` to the viewport rect explicitly, and the smoke gained a geometry assert (HUD spans the viewport) so it can't regress — **200 ok-checks**. **HUMAN: every HUD colour/size/timing is a PLACEHOLDER constant** grouped at the top of `run_hud.gd` (palette, bar/tile/slot sizes, vignette shape, pickup hold/fade seconds) — dial like FEEL numbers (spec + deferred work: `design/ui-hud.md`; mockups in the claude.ai/design "Tycho UI" project). **The boss bar + the low-HP vignette/tint shipped with this.** Deferred: echo-tile tooltips / a hold-Tab detail view, the town HUD restyle, the pause/panel restyle to the slate language, a cooldown pie-wedge.
- **ESC pause menu — Resume / Forfeit Run / Save & Quit, Hades-gated, plus a statistics guarantee (2026-07-07, human request):** ESC (`ui_cancel`) opens a pause menu — new **`PauseMenu`** (`src/core/pause_menu.gd`), spawned once by `game.gd` onto the HUD layer beside the cheat panel (survives scene swaps), `PROCESS_MODE_ALWAYS`, hidden by default; toggling pauses/unpauses the tree. Guards: **inert at slot select** (`game.gd.on_slot_select()`) and it **won't hijack ESC while another panel already owns the pause** (dialogue / echo offer / F1 / F2 / tech / forge / etchings — only its own open state may unpause). **The Hades quit-gate:** a 1-line room method **`combat_room.can_menu_quit()` = `_cleared or not _damage_taken`** — in a run you may Forfeit or Save & Quit only after clearing the room OR while still untouched by it (`_damage_taken` is set in the existing `_on_player_health_changed` when hp drops; the tracker is rebaselined right after carried-HP setup so a wound carried IN from the last room doesn't falsely close the gate); reprieve/auto-clear rooms are trivially allowed; town (no such method) is always allowed. **Forfeit Run = "like it never happened":** `game.gd._start_run` first snapshots the whole slot at portal entry (`_run_snapshot`, live Ledger captured explicitly since `state.ledger` only syncs on save); Forfeit aborts RunState via the new **`RunState.abort_run()`** (clears the run emitting NOTHING — no `run_ended`/`death`/`boss_killed`, so no day tick, no counters, echoes gone), restores the snapshot to memory AND to the live Ledger, forces `checkpoint` null, saves (overwriting the run's on-disk floor checkpoints), then returns to town the normal deferred way — resources/counters/checkpoint exactly as at portal entry, on disk and in memory (the in-session `run_number` may skip a value — cosmetic). **Save & Quit** returns to the slot-select screen (`game.gd._return_to_slot_select` reusing `_show_slot_select`; title music rides it; `choose_slot` works again = reload-from-disk → resume the checkpoint or town). **The statistics invariant (the reason the gate exists):** `choose_slot`→`load_slot` always re-reads the slot from DISK, and the only mid-run writes are the per-floor checkpoint snapshots — so a counter earned after the last checkpoint (e.g. the final-boss `boss_kills` in the kill→pedestal window) is never persisted; quitting discards it and the resume re-earns it **exactly once**. This holds ONLY while nothing writes to disk mid-run outside that snapshot, so **in-run Save & Quit writes NOTHING to disk** (the checkpoint IS the save). The invariant is stated in comments where the menu quits AND proven in the smoke (disk `boss_kills` < memory `boss_kills` in the final-boss window). No pure core extracted (the gate is a trivial room method) → **137 test cases green, unchanged**. Smoke extended: the statistics-window disk-lag check; the run-2 gate (a hit → `can_menu_quit` false + `pause_menu.forfeit()` refuses, no counter change → clear the room → true); a run-3 Forfeit (gold/runs/checkpoint roll back on disk + in memory); a town Save & Quit round-trip (→ slot select → `choose_slot` → same counters) — **193 ok-checks green** (was 172; the ±1 door-sigil nondeterminism still stands). Editor pass clean. No save-schema change (the snapshot is in-memory). **HUMAN: the button labels + the gate-refusal line ("Finish the fight first (or leave before taking a hit).") are placeholders — dial the copy freely (`PauseMenu` consts/strings).**
- **The full-clear "win ending" is built — the codex artifact dissolves Tycho home (2026-07-07, human decision; SUPERSEDES the a3-first-return bullet below):** defeating the FINAL boss now reveals a **codex-artifact pedestal** in the chamber (the only way out) after the boss valve (heal + guaranteed echo); walking into it plays a **placeholder dissolve FX** (tinted death-burst reuse) and ends the run VICTORIOUS through the exact existing `run_ended(true)` path — the run's codex shard slots in on the town return. Happens on EVERY full clear. The dissolve ticks a NEW counter **`dissolves`** (`EventBus.dissolved` → `StoryState._on_dissolved` → `StoryCore.record_dissolve`), SEPARATE from combat `deaths` (which stays combat-only). **Codex now clamps:** `StoryCore.grant_codex_shard` caps at new const **`CODEX_SHARDS_MAX := 6`** (placeholder, PRD's 5–7 range) — a grant at max is a no-op. At max the **E2 cutscene** ("The artifact waits") force-plays. **The A3 opener twins are re-gated death-vs-dissolve:** `a3-first-return` (`runs>=1`) is **DELETED**; NEW **`a3-first-death-alt`** (gate `dissolves>=1`, priority 104, `sets_flag: a3`) is a **byte-exact scene copy** of the human-verbatim `a3-first-death` (105, `deaths>=1`) — per the human, both twins keep the SAME dialogue incl. the dying references. A first run either dies or dissolves, so the gates never both hold; shared-flag suppression silences the other. NEW **`e2-artifact-waits.json`** (spine force-play, gate `codex_shards>=6` COUPLED to `CODEX_SHARDS_MAX`, sets `e2`, priority 98) — **placeholder text, flagged for human curation** (drafts-review-2026-07-06.md §6). **Flow:** `game.gd._on_room_cleared` branches on new `RunState.is_final_boss()` → `RunState.final_boss_killed` (emits boss_killed, holds the run open) + valve + echo → `_open_artifact` → pedestal (combat_room `open_artifact`/`_spawn_artifact`/`_dissolve_player`, group `codex_artifact`) → walk-in emits `dissolved` + `artifact_entered` → `RunState.finish_at_artifact` advances to victory + `run_ended`. `cheat_panel.simulate_run(true)` now also emits `dissolved` (its contract: the exact signals a real full clear emits). Non-final floor bosses are UNCHANGED (plain exit after their valve). Save: `story.counters.dissolves` added (defaults-merge covers old saves). Tests: `story_core_test.gd` +dissolve-increment +codex-clamp; `dialogue_core_test.gd` twin test rewritten to death-vs-dissolve + a generic dissolves-counter assert → **137 test cases green** (was 135). Smoke drives the pedestal walk-in (final boss → dissolve), asserts `dissolves==1`/`deaths==0`/codex==1 on the run-1 return, the run-1 force-play is now `a3-first-death-alt` (deaths still 0), the clamp (grant past 6 stays 6), and E2 force-plays at max shards setting `e2` → **172 ok-checks green** (was 164; ±1 door-sigil nondeterminism known). Editor pass clean. **HUMAN: `CODEX_SHARDS_MAX` (6) is a placeholder — dial it (StoryCore const; the E2 gate `codex_shards>=6` must move with it). E2's lines want your pen (register: scientific-observing, no-BS). The dissolve FX is a placeholder (tinted burst + hide) awaiting the painterly pass.**
- **Etchings first slice — the RMB/Q/R ability kit is playable (2026-07-07, design/etchings.md first implementation):** Tycho's "magic" loadout is live end-to-end. **All 9 abilities ship as data** (`data/etchings/*.json`: `slot`/`principle`/`cooldown_s`/`granted_by`/dust costs/`levels`/`weapon_synergy`/`summon_seed` + a per-ability `behavior` dict = the dial board; DataLoader `ETCHINGS_SPEC`, filename==id, loud validation). **Five are IMPLEMENTED and castable** — **Push** (RMB cone knockback; slamming an enemy into a wall deals bonus damage + a brief forced stagger), **Bolt** (RMB ranged dart, reuses `arrow.gd`, staggers squishies), **Snare** (Q ground field slowing enemies via a new `EnemyDummy.slow_factor` hook, restored on exit/expiry; squishies briefly rooted on cast), **Shockwave** (R radial damage w/ falloff + knockback that **force-staggers even armored enemies** — the ONLY thing that bypasses `stagger_time==0`), **Surge** (R ~5s self-overdrive: move/attack-speed up, dash CD halved; restores the exact pre-cast values, never raw exports). **Four are DORMANT** (Afterstrike/Ward/Lodestone/Sentinel — data ships, the panel shows them "the resonance does not answer this one yet", not learnable; **implementation status is CODE, not data** = `EtchingsCore.IMPLEMENTED`). **Deferred (document-don't-build):** L3 riders (data only), weapon synergies (data only), etching-mod echoes, attunements. **Pure `EtchingsCore`** (`src/learning/etchings_core.gd`, return-a-new-dict like TechCore) owns unlock/level/cost/equip + `ensure_baseline` (grants Push L1 + auto-equips it to RMB when flag `b2` is set — idempotent; called on panel open + player spawn) + `effective_behavior` (folds each level's `<field>_mult` into the base behavior). **`EtchingsPanel`** (`src/town/etchings_panel.gd`, code-built pause UI in the forge/tech mold) opens from **Thomas's meditation spot** — `town.gd _try_meditate` now calls a new `open_etchings_panel()` (re-checks `UnlocksCore.is_unlocked(…,"etchings")`, null when locked, keeps the "not yet" flash; the old one-line meditation beat is retired). **Player casting framework** (`player.gd`): loads equipped slots at spawn after `ensure_baseline`, per-slot cooldowns, polls `ability_rmb/q/r`, public `try_cast(slot)->bool` (fires toward facing, never mid-dash); new FX (`CombatFX.shockwave_ring`, `SnareField`) + `EnemyDummy.apply_knockback`/`stagger`/`force_stagger`/`is_staggered`/`slow_factor`; minimal 3-slot cooldown HUD readout in `combat_room`. **No save-schema change** — `combat.etchings {slots,unlocked}` already existed. Dust is spent via `Ledger.try_spend(…, "etching")`. Tests: `etchings_core_test.gd` (+9 — domain lint, implemented/dormant split, cost progression, dormant gate, learn/equip rules, baseline idempotency, level-mult math) → **135 test cases green** (was 126). Smoke: B2 + learn-a-kit relocated BEFORE run 2 so the run player carries it; asserts baseline Push, learn Snare+Shockwave (10 Dust spent), dormant Sentinel refuses, disk persistence, then in-run `try_cast` proves Snare slows a dummy + Shockwave force-staggers an armored Brute + both engage cooldown — **164 ok-checks green** (was 144–145; ±1 pre-existing door-sigil nondeterminism). Editor pass clean. **HUMAN: every ability number is a placeholder — dial like FEEL numbers:** the `behavior` dicts + level `_mult`s + dust costs + cooldowns in `data/etchings/*.json` (dial board in `design/feel-tuning.md`); no dedicated cast SFX yet (reuses `dash` as a placeholder whoosh — add per-ability rows to `data/audio/sfx-map.json` later). Riders/synergies/dormant-four are the next etchings chunk.
- **A3 opening scene now fires win OR lose — the win-first-run edge is closed (2026-07-07, human decision):** the opening cutscene (find Sophia → the portal refuses her) was gated `deaths>=1`, so a player who never died on run 1 stalled `a3` and everything hanging off it (Mara's first-meeting, Thomas/B2 → the etchings unlock). Fixed with a **twin pair** reusing the existing shared-flag suppression (the B3 pattern): **NEW `data/dialogue/a3-first-return.json`** — the generic opener, force-play cutscene, gate `runs>=1`, **priority 104**, `sets_flag: "a3"` — fires on the FIRST run's town return regardless of death; **`a3-first-death` UNCHANGED** (gate `deaths>=1`, **priority 105**). After a death both gates hold (a death still ticks `runs`) so priority 105 wins and the death version plays; after a deathless first run only the generic is eligible; either sets `a3` and DialogueCore's flag-suppression silences the other forever. `a3-first-return`'s lines reuse the human's own `a3-first-death` text byte-exact wherever the scene is identical (finding Sophia, the walk, the portal refusal, the close); the three no-death-specific lines + two death-clause-trimmed lines are **orchestrator placeholders flagged for the human** (see `design/dialogue/drafts-review-2026-07-06.md` §6). **Smoke resequenced** (the whole cascade shifted one force-play slot earlier): the generic A3 now claims the **run-1 victory return** (was B3); B3 defers to the next town entry (a sim, which also carries the Food-upkeep day tick now that the final B5-only sim is dropped); B5 force-plays on the **run-2 death return** where `a3-first-death` is twin-suppressed (the suppression proof — B5 plays though its priority 95 < a3-death's 105, because a3-death is inert). New coverage: the win-first-run A3 twin, Mara's greeting-then-ore ordering, and a silent (no-force-play) sim return. Total sims held at 2 → end counters unchanged (runs 4, full_clears 3, deaths 1, codex 3→4). Tests: `dialogue_core_test.gd` +`test_a3_twin_pair_priority_then_suppression` → **126 test cases green** (was 125). Smoke **144–145 ok-checks green** (was 143; ±1 is the pre-existing door-sigil nondeterminism under the `randi()` run seed). Editor pass clean. **HUMAN: overwrite the 5 placeholder lines in `a3-first-return.json` (drafts-review §6) — the rest is your verbatim text.**
- **Tech-tree content-gate fixes — shard turn-in, quiz-fail-waits-a-run, wiki-register texts (2026-07-06, from the human's content-gate playtest):** three changes to how research reads and pays, all built. **(1) Shard economy split:** **investing now spends Knowledge ONLY** — the mid-invest shard auto-conversion is gone. Knowledge Shards (boss loot) are the thing you BRING Sophia and **turn in at the desk** for Knowledge: a new **`TechState.turn_in_shards()`** owns the atomic Ledger transaction (spend `knowledge-shards` + add `knowledge`, both reason **`shard-turn-in`**; whole shards at `SHARD_KNOWLEDGE_VALUE`=5, unchanged). The tech panel gained a **"Turn in N Knowledge Shards → M Knowledge"** button (list + node screens, disabled at 0). **Removed `TechCore.shards_needed`** (the old gap/ceil conversion math) + its test; new pure **`TechCore.shard_turn_in_value`** covers the trivial turn-in math. Diegetic: you bring her what you found, she extracts what it teaches. **(2) Quiz fail → wait one run:** a WRONG quiz answer now **locks that node's quiz** (`tech.quiz_locked {id:true}`, new save field; defaults-merge covers old saves) until one more run passes — **`TechState._on_run_ended` clears all locks each run** (single-subscription, alongside the auto-solve tick). Panel shows a **LOCKED screen** (Sophia: "That's not it. Go make a run and clear your head. Ask me again when you're back.") with no answer buttons; `answer()` no-ops while locked. **Only the `quiz` kind locks** — the interactive arch puzzle's staged failures ARE the teaching and stay freely retryable. Philosophy intact: a wrong answer DELAYS, Sophia still auto-solves after ~5 runs (never hard-gate, IC-10). Pure `TechCore.lock_quiz`/`is_quiz_locked`/`clear_quiz_locks`. **(3) Wikipedia-register rewrites:** the player-facing explanation/aha in `data/tech/med-arithmetic-zero.json` + `med-masonry-arch.json` rewritten to a **neutral encyclopedic voice** (declarative, third-person, no em dashes, no rhetorical questions, no second-person pep) — this IS the "aliens' wikipedia". Facts unchanged (positional notation/zero; lintel tension-failure → voussoirs/keystone/centering → outward thrust/buttress → pointed arch; the catenary aha, still framed as knowledge the 1157 masons didn't have). Quiz phrasing flattened (caps emphasis gone). Authoring source `design/tech-nodes/medieval-masonry-the-arch.md` §4/§5/§6 synced to match. Sophia's arch-puzzle **hints + intro are dialogue** — retouched only to drop em dashes/shout-caps (logged in `design/dialogue/drafts-review-2026-07-06.md` §5). Cheat panel grant/research paths unchanged. Tests: `tech_core_test.gd` −`test_shards_needed` +`test_shard_turn_in_value` +`test_quiz_lock` → **125 test cases green** (was 124; net +1). Smoke's research path restructured to prove the mechanics: turn shards in (knowledge +5×shards, pouch emptied), invest spends knowledge only (parked shards untouched), a wrong answer locks the quiz (no retry), a simulated run clears the lock, then the correct answer researches — **143 ok-checks green** (was 134; the extra quiz-gate sim bumps run/clear/codex counts, downstream asserts updated). Editor pass clean. **HUMAN: `SHARD_KNOWLEDGE_VALUE` (5) stays a tuning placeholder; the quiz-lock is "exactly one run" (no cap on retries beyond that) — dial if playtest wants longer/shorter.**
- **Opening dialogues + voice recalibration — the human's first scripts are in (2026-07-06, human-authored text is canonical):** the human wrote the game's opening dialogues (first death → find Sophia → the portal refuses her) and the first-meeting beats for Mara/Thomas/Herzog, and set the register — **"scientific observing, no-BS. no poetic / wise stuff (Thomas excepted)."** Integrated verbatim (typo fixes only). **`data/dialogue/` +3, ~3 edited:** **`a3-first-death`** (spine, **force-play cutscene**, gate `deaths>=1`, sets `a3`, **priority 105** so it takes the one force-play slot ahead of B5 on the death-return visit; stage directions became telegram narration lines matching b5's `who:""` format — "They walk to the portal." / "Sophia steps toward the portal. It does not let her through."); **`a-mara-meets`** (spine, gate `flag(a3)`, priority 96 so Mara's greeting precedes her ore beat); **`b2-thomas-meditation`** (spine, gate `flag(a3)`, **sets `b2`** → the etchings system + the meditation spot; B2's gate thus moved from the skeleton's "first Dust + visited Thomas" to `flag(a3)` per the human); **`b4-herzog-ledger` reworked** to the human's lines (same id/gate/flag, 4→3 lines). Typo fixes reported: "chick in"→"check in", "ecounter"→"encounter", "ressources"→"resources"; grammar/missing-periods left verbatim; no em dashes anywhere. Two voice-guide tensions kept per the human (Tycho's "data points", Sophia's "divine energy") and recorded as licensed exceptions. **Sophia is now a town NPC** (`NpcSophia` near her desk, talkable day 0 — her arc beats had NO talk spot before, the desk opens the research panel not dialogue, so they were unreachable). **`!`/`!!` banner icon built** (PRD §7.12's "banner icon" cashed in): a floating Label3D over any NPC with **new UNSEEN** content — `!!` spine/main-story, `!` arc/contextual; barks + seen content never light it (indicator = new content only). Logic is pure **`DialogueCore.indicator_for`** (unit-tested); `town.gd` renders + refreshes on entry and after any dialogue closes. **Meditation spot** (Area3D + prop near Thomas, gated on `etchings`←b2 like the forge/desk): locked = a "not yet" flash, unlocked = a one-line diegetic beat ("Tycho listens to the resonance. Nothing answers yet.") — the etchings/attunement MENU is document-don't-build. **Voice recalibration (`voice-guides.md`):** house style sharpened to scientific-observing/no-BS; the human's scripts added as the top calibration anchors (outrank all prior samples); Thomas keeps a limited contemplative license; Sophia's tell revised (curious-empirical, not schoolmarmish); the deniability rule gained the Tycho "data points" exception. **Conformance pass** over all 46 `data/dialogue/` snippets to the new register — the batch was already anti-aphorism so most were conformant; **2 files rewritten** (`wren-bark-roads` mystical tail; `b5-the-first-wall` narration em dash + "It is a beginning" inspirational button), borderline-kept ones flagged in the review file. No save-schema change (story flags are a free dict). Tests: **124 test cases green** (was 122; +`indicator_for` cases + a forced-priority case). Smoke resequenced (the first-death cutscene now consumes run-2's force-play slot that B5 used to; B5 deferred to the next town entry = the simulated run; new coverage: A3 plays+sets `a3`, Sophia NPC talkable, Thomas B2 sets `b2`+etchings unlock+meditation spot, Herzog A4 with talked_to, and a `!!`→`!`→cleared indicator case on Tilly) — **134 ok-checks green** (was 120). Editor pass clean. **Deferred/flagged for the human:** the **win-first-run edge** (a player who never dies never trips `deaths>=1`, stalling A3 and its a3-chain incl. etchings — a fallback like `OR runs>=N` is a human call, noted in act1-story-beats.md); the greeting-after-ore ordering edge; the etchings menu itself. **HUMAN: curate the opening scripts + the conformance changes in `design/dialogue/drafts-review-2026-07-06.md`.**
- **Waves + two new enemy types — combat rooms now fight like Hades (2026-07-06, playtest feedback: "more enemies, in waves" + "more attack patterns, e.g. AoE after windup"):** the single-wave combat room is retired. **Combat rooms now run 2–3 SEQUENTIAL waves:** kill the wave → a short beat (`wave_beat` 1.0s) → a **spawn telegraph** (a growing ground marker flashes `wave_spawn_telegraph` 0.6s at each point before the enemy appears) → the next wave; the room counts as CLEARED **only after the last wave**, so ALL existing on-clear logic (door offers, echo offers, caches via `door-reward`, Wellspring/reprieve, boss valve, exit) fires exactly once, unchanged, on the final clear. **Total bodies per room went UP** (each later wave adds a body over the old `base + floor + room` count). Composition is a **pure, seeded, unit-tested `WaveCore`** (`src/combat/wave_core.gd`): `compose(floor, room, base_count, seed)` → a list of waves, each an `Array[String]` of type-ids; deterministic in the run seed (so the smoke can force a known mix); placeholders = `MIN_WAVES 2 / MAX_WAVES 3 / THIRD_WAVE_AT 4` (a room earns its 3rd wave once floor+room≥4), the `wave_size` growth curve, and `TYPE_WEIGHTS` (Brute 3 / Skirmisher 3 / Archer 2 / Slammer 1 / Charger 1). `combat_room.gd` maps ids→scenes, spawns wave-by-wave, and **peril mults (DoorCore.peril_hp/peril_damage) apply to EVERY wave's spawns** (they ride `_spawn_enemy`, called per wave). **Reprieve rooms and boss rooms are untouched** (boss = the single staged fight as before; no waves added to bosses this chunk). A **"Wave N/M" HUD hint** shows in multi-wave rooms (reuses the room's hint label). **Two new enemy types** (both `extends EnemyDummy`, override engage/strike like the Archer; placeholder primitives, `resource_local_to_scene` mats, all stats `@export` `# FEEL:`): **Slammer** (`enemy_slammer.gd`/`.tscn`, steel-blue, tanky/armored `stagger_time 0`) — lumbers in, LOCKS an impact point and paints a **growing ground-circle telegraph** (new reusable `CombatFX.ground_telegraph`, also used for the wave-spawn markers) over a long 0.9s windup, then slams: AoE damage if the player is inside `slam_radius` at impact — **dashing out dodges it** (the impact is locked at telegraph start; dash i-frames also cover it); participates in the melee commit-token pool. **Charger** (`enemy_charger.gd`/`.tscn`, teal, squishy) — at range locks on + paints a brief **line telegraph** (`CombatFX.line_telegraph`), then dash-charges down it at `charge_speed` 22, damaging on contact; overshoots and is left stunned + vulnerable (`recover_time` + `wall_stun_bonus` if it slams a wall); **staggerable except mid-charge** (take_damage suppresses the stagger-interrupt only during the charge — the charge is its commit). Both wired into the WaveCore mix (from floor 1, lightly weighted) and into the F6 sandbox rotation (`feel_room.gd` `_scene_for` now `i % 5` — the ONLY feel_room change; `feel_room.tscn` untouched). **No new audio** — the enemies reuse existing sfx-map ids where they fit; a dedicated slam/charge whoosh is noted for the human (see below). Tests: `wave_core_test.gd` (+7 — determinism, wave-count range, 3rd-wave-deeper, total-beats-old-single-wave, valid-types-only, both new types reachable from floor 1, wave sizes grow) — **122 test cases green** (was 115). Smoke updated to iterate multi-wave rooms (a `_kill_room` loop that clears every wave), asserts a real room ran >1 wave, and validates both new scenes load + extend EnemyDummy + carry their signature exports — **~119–120 ok-checks green** (was 113; the ±1 is the pre-existing door-offer-distinct-sigil check, and the run seed is `randi()`). Editor pass clean. **HUMAN: everything numeric here is a placeholder — dial like FEEL numbers:** wave counts/sizes/`THIRD_WAVE_AT` + `TYPE_WEIGHTS` (`WaveCore` consts), `wave_beat`/`wave_spawn_telegraph` (combat_room exports), and every Slammer/Charger stat (their `.tscn` exports; full dial board in `design/feel-tuning.md`). Tune them in the F6 sandbox (all five variants now rotate there) and in-run (F1). If the slam/charge want their own sound, add rows to `data/audio/sfx-map.json` + a hook (deferred — no new audio synthesized this chunk).
- **Run structure — door choice + the in-run healing economy built (2026-07-05, design/run-structure.md first implementation):** the meso-loop's decision layer is live and the echo-cadence retirement is done. **Pure `DoorCore` (`src/core/door_core.gd`)** lays out a whole floor's **door plan** deterministically from (run seed, floor, rooms_this_floor, floor profile): per room-transition an offer of **1-2 doors** (weighted single-door chance = "no fake choices"), each `{sigil, peril}`; sigils `gold/ore/dust/echo/reprieve` (+ a single **boss door**, always last, never peril); **never two identical sigils per offer**, **pity ≥2 echo + ≥1 reprieve doors/floor** (degrades gracefully by capacity on short floors), **no sigil 3× consecutive** (left-to-right draw is triple-safe by construction, pity conversions re-check). The plan is a plain list — the **Cartography foresight hook** is trivial later (NOT built; the tech node doesn't exist). DoorCore also owns the cache/peril/heal math (all pure). **New `data/floors/` domain** (`1.json`…`5.json`: `door_weights` + `peril_chance` rising 0.10→0.30 with depth; DataLoader `floors` spec, filename==id, strata env fields land here LATER — additive) and **`data/resources/resonance-dust.json`** (source `door-reward`; its etchings/attunement sinks aren't built, it just accumulates). **Rewards on clear, by the sigil that ENTERED the room:** gold/ore/dust caches via `Ledger.add(…, "door-reward")` (placeholder amounts in DoorCore, scaled by floor, **peril doubles**), echo sigil = an echo offer, reprieve = a **Wellspring** (Area3D prop, heal **40% of MISSING HP** through the real player API). **Boss valve:** every floor-boss kill auto-heals **30% of missing**, then a **guaranteed echo offer**, then the exit. **Echo cadence CHANGED (the doc's decided retirement):** the every-combat-room offer is GONE — echoes now come only from echo-door rooms + the post-boss guarantee (game.gd `_on_room_cleared` reworked). **Peril = the elite-modifier stub (PRD §7.7 first concrete use):** spawned enemies get runtime `max_hp ×1.5 / attack_damage ×1.25` applied AT SPAWN over the exports (never a .tscn edit). **Doors UI (placeholder):** on clear, 1-2 door portals with a Label3D sigil + peril mark (`⚠`); walking into one commits it (no backtracking), game.gd carries the chosen `{sigil,peril}` on **RunState** (`door_plan` + `pending_door`, NOT saved — the checkpoint resumes at floor start and the plan regenerates identically by seed). **Deferred (document-don't-build):** Cartography foresight, the 2-3 healing echoes (need heal hooks the echo-mod system lacks), Recovery attunement (no attunement system), 3-door offers, per-floor heal caps, first-encounter door tooltips (with the painterly pass). Tests: `door_core_test.gd` (+11 — determinism, offer sizes, distinct sigils, boss-door invariants, pity on a normal floor + graceful degrade on a 2-room floor, no-3-consecutive across 80 seeds, peril bounds, cache/peril/heal math) — **115 test cases green** (was 104). Smoke (5 rooms/floor so pity guarantees a reprieve door) picks doors, asserts a dust cache paid on clear, the post-boss echo, the 30%/40% heals, and the floor-2 door plan regenerating identically across the mid-run quit/resume — **113 ok-checks green** (was 100). Editor pass clean. **HUMAN: door weights, peril chances, cache amounts, peril mults, and the heal percentages are all placeholders — dial them (data/floors JSON + DoorCore constants) like economy/feel numbers.**
- **Food upkeep — the Well-Fed mechanic is built (2026-07-05, design/food-upkeep.md first implementation):** Food was a dead resource (it didn't even exist in `data/resources/`); it now has its one v1 job. **`data/resources/food.json`** (role `material`, source `farm`) + **`data/buildings/farm.json`** (ungated by tech like `sophias-study` — agriculture is day-0; the build *system* is already B4-gated, which covers it — L1/2/3 produce 3/5/8 food, **L1 = 40 gold** so it ties the cheapest-L1 cost the B4 gate documents). **The upkeep pass lives in pure `TownCore.tick` (`src/town/town_core.gd`), now `tick(town, building_defs, food_stock) -> {produced, food_consumed, well_fed}`:** raw production first (incl. the farm's food), then `upkeep = UPKEEP_BASE(2) + UPKEEP_PER_BUILDING(1)×built` (constants, commented economy placeholders — NOT `# FEEL:`), then `available = food_stock + produced_food` (the harvest comes in before the town eats), `well_fed = available >= upkeep`, consume `min(upkeep, available)` (eats what it has when short — the balance never goes negative), and if well_fed every produced amount EXCEPT food gets ×1.25 (`WELL_FED_BONUS`). **Never a penalty — short on food is just no bonus, no starvation** (schemas §6 tick order: produce → upkeep/status → status-modified totals). **`game.gd`'s day tick** passes `Ledger.get_amount("food")`, adds production, then `Ledger.try_spend("food", food_consumed, "upkeep")` AFTER (stock math matches "harvest first"), and stores `state["town"]["well_fed"]`. **Town UI:** a Farm build plot in `town.tscn` (metadata-generic, zero town.gd plot-code change) + a one-line Well-Fed / Short-on-food indicator by the day label (reads `state.town.well_fed`, shown once a day has ticked) + "food" added to game.gd's `HUD_RESOURCES`. **Save:** `save_data.gd` town defaults gain `"well_fed": false` (defaults-merge covers old saves). **NOT in scope:** no dialogue lines (voice-guides curated batch is separate). Tests: `town_core_test.gd` +5 upkeep cases (upkeep math per building count, coverage boundary, partial-consume-when-short, bonus-on-others-never-food, empty-town-no-error) — **104 test cases green** (was 99). Smoke builds the farm + drives a well-fed day tick and asserts the food math + `well_fed` persistence — **100 ok-checks green** (was 96). Editor pass clean. The Act-II prepare-for holds: the same consume-then-status pass generalizes to army/city provisioning (only the numbers scale).
- **TechState autoload migration — tech bookkeeping leaves game.gd (2026-07-05):** the last target-only state autoload is now a real owner — an internal refactor mirroring the StoryState pass, **zero behavior change** (same save contents, same signal emissions in the same order, same Ledger reasons; smoke identical plus new disk asserts). **No new pure core** — `TechCore` (`src/learning/tech_core.gd`) already IS the tested pure core and is untouched; only the scattered mutation sites moved. Thin **`TechState` autoload** (`src/autoload/tech_state.gd`, registered right after `StoryState`, before `RunState`) owns every mutation of `SaveManager.state["tech"]`: **`set_active(id)`** (node selection), **`invest(def, id)`** (the Knowledge/Shard transaction incl. its Ledger spends — kept atomic with the write, exact behavior/reasons preserved), **`complete(id)`** (`TechCore.complete` + `EventBus.tech_researched.emit` — now the SINGLE home of that pair, previously duplicated in the panel, the cheat panel, and game.gd's auto-solve), and Sophia's **run_ended auto-solve** (moved verbatim out of game.gd; the complete step routes through `complete()`). game.gd's `_on_run_ended` keeps only scene flow (checkpoint clear, day tick, deferred town swap + save) — the auto-solve block and its comment are gone. **Explicitly left in game.gd:** the `_on_tech_researched` age-advance hook (mutates town/meta.age, not the tech section — same reasoning as the day tick). Call sites: `tech_panel.gd` (`select_node`/`invest_all`/`finish`) and `cheat_panel.gd` (`research`) now route through TechState. **Style note:** TechCore is return-a-new-dict (unlike StoryCore's in-place), so every TechState mutation reassigns `state["tech"]` and call sites must re-fetch it after any call (same hazard class as DialogueCore.mark_shown; documented in the header). **Ordering guarantee:** TechState is an autoload, so it subscribes to run_ended BEFORE game.gd — the auto-solve result is in state before game.gd's deferred town-return save persists it. Tests: **99 test cases green** (unchanged — TechCore's suite stays green untouched, no new core to test). Smoke **96 ok-checks green** (was 93; +3 disk asserts re-reading the slot file to prove the TechState-mutated tech dict lands on disk: `tech.researched` persisted, `active` cleared on completion, completed node's in-progress/counter cleared). Editor pass clean. Docs synced: godot-conventions (autoload list + directory tree + milestone), architecture-schemas (§4 implementation status), story_state.gd header (tech auto-solve no longer listed as game.gd's).
- **Unlock cascade — town systems now come online one at a time (2026-07-04, PRD §7.1):** the "everything open from minute 0" gap is closed. Pure **`UnlocksCore`** (`src/core/unlocks_core.gd`) — a `SYSTEMS` map (weapons←`b1`, etchings←`b2`, tech←`b3`, building←`b4`) + `is_unlocked(state, system)` reading `story.flags`; **unknown system id → push_error + false** (a typo never silently opens a facility). **Town gating** (`town.gd`): Mara's Forge (`open_forge_panel`→null when locked), Sophia's Desk (`open_tech_panel`→null), and every build plot (`_try_build`) re-check on each press; locked = a brief `"<who>: not yet."` flash + a diegetic label (`"Mara's Forge — locked (Mara hasn't offered her services)"` etc.); flags re-checked after any talk (a beat can open a facility mid-visit). **The dungeon portal, NPC talk spots, and the dialogue system are NEVER gated** (talking is how beats fire). **etchings (`b2`) is mapping-only — the system isn't built, so nothing is gated or authored there.** **Two cascade beats authored** (voice-guides.md, human-curation-flagged in `design/dialogue/drafts-review-2026-07-04b.md`): **`b3-sophia-shards`** (spine cutscene, force-plays, gate boss_kills≥3, sets `b3`) with its **fallback twin `b3-sophia-shards-alt`** (identical scene, gate runs≥6, same `b3`) — the act1-story-beats.md B3 "OR runs≥6" fallback realized as a **second copy**; **`b4-herzog-ledger`** (spine talk at Herzog, gate gold≥40 = the cheapest L1 building cost, sets `b4`). Existing `herzog-ledger-open` contextual re-gated `+flag(b4)` (its first line retouched — the ledger is already open post-B4). **Twin-suppression:** the AND-only condition vocabulary has no negation, so DialogueCore's `eligible` gained a tiny general rule — **a snippet whose `sets_flag` is already set is inert** — so the two B3 copies can't both play (once either sets `b3`, the other is suppressed; also protects any future twin-gated beat). **Cheat panel** gained a "Set story flag (raw)" row (b1–b5, ✓ shows current state) — the F2 no-grind promise must survive the cascade. Tests: `unlocks_core_test.gd` + DialogueCore twin-suppression cases — **88 test cases green** (was 80). Smoke drives the whole cascade (fresh save all-locked → the run's 3 boss kills force-play B3 → talk Mara for B1 → talk Herzog for B4 → build/research/forge as before) — **88 ok-checks green** (was 70); the smoke's run is now 3 floors so one clear trips the B3 gate. Editor pass clean. **HUMAN: old dev saves now lock retroactively** — a save without `b1/b3/b4` will find the forge/desk/plots shut; reopen them with the F2 "Set story flag (raw)" row, or just replay the beats (talk to Mara, clear to the 3rd boss, talk to Herzog).
- **Dialogue content batch 1 — arcs + barks drafted and integrated (2026-07-04, HUMAN CURATION PENDING):** the first voice-guides-driven drafting pass (an Opus subagent wrote it; every line checked against the banned-constructions list, per-character tells, and the tech-unlocked vocabulary table). **38 snippets landed in `data/dialogue/`:** 17 character-arc beats (Sophia 4, Tilly 4, Mara 4, Thomas 5) + 1 Sophia tech-vocabulary contextual (uses "zero" only behind the Arithmetic gate) + **20 barks** (Herzog 6, Mara 5, Tilly 4, Wren 5; `cooldown_runs` 2–3). **Full review table + flags: `design/dialogue/drafts-review-2026-07-04.md`** — the human's curation pass starts there (prime candidates: `sophia-arithmetic-zero` delight-vs-homework; the Thomas "two lamps" anchor was recast into his question-tell because the approved line is itself a mirrored aphorism — accept or revert). **Thomas got his town talk spot** (`NpcThomas` in `town.tscn`, south edge; `town.gd` unchanged — NPC pickup is generic), so his 5 beats are reachable. **Em-dash compliance fix on 3 shipped files** (b1/c3/herzog-ledger predate the voice-guide lock; 5 spoken lines re-punctuated, b5's narration em dash left — the ban reads as dialogue-only). Known dormant-by-design: `arc-sophia-telescope` STUBBED (needs an Optics tech node + the human pinning her discovery — stub NOT in data/); `arc-mara-cannon` gates on unauthored `ren-gunpowder`; several beats gate on unauthored spine flags (c2/c4/d3/d4/b3) — correct chaining, testable via F2 once those exist. The review also records why ~7 beats/arc wasn't padded to: the beats doc pins trajectories, not 7 gated definitions — defining the rest is design work, not drafting. Validation: 80 test cases + smoke 70 checks green post-integration (spine>arc>contextual kept B5/A4 smoke beats intact).
- **Placeholder music — the `Music` autoload is live (2026-07-04):** audio.md's first-chunk step 3 built, so the audio first chunk is now complete (both halves). **4 seamless placeholder loops** in `assets/audio/music/` (`title`/`town`/`dungeon`/`boss`, `.ogg`, `loop=true` pinned in each `.import`) — **agent-synthesized, NOT FreePD** (deterministic pure-python additive synth → ffmpeg libvorbis q4; seamless-by-construction: every oscillator an integer number of cycles over the loop, rhythmic envelopes zero at the seam, no edge fades — same placeholder rationale as the SFX; provenance in `assets/audio/SOURCES.md`, swapping a track = editing its `music-map.json` row). Pieces: pure **`MusicCore`** (`src/core/music_core.gd` — `resolve` id → {file, volume_db} + the equal-power `crossfade_gain(t)` sin/cos curve) under thin **`Music` autoload** (`src/autoload/music.gd` — two `AudioStreamPlayer`s on the `Music` bus, PROCESS_MODE_ALWAYS like Sfx, ~1 s equal-power crossfade driven per-frame in `_process` **not a Tween** (Tweens mis-time under pause), mapped `volume_db` as the fade ceiling; `play(id)` no-ops on the current id, warns-once on unknown; `current_id` exposed; streams preloaded at boot); **`data/audio/music-map.json`** via `DataLoader.load_music_map()` (single-file map like sfx-map, shared `_load_single_file_map` reader, `MUSIC_SPEC`-validated). **Profile audio settings now exist in code:** `save_data.gd` profile defaults gained `settings.{music_volume,sfx_volume,ui_volume}` (linear 0..1, defaults-merge covers old profiles), applied to the Music/SFX/UI bus volume_db in `Music._ready` (data-only — no settings UI yet, audio.md says the UI can lag). **Hooks in `game.gd`** (one-liners beside the scene swaps): slot-select → `title`, town → `town`, run rooms → `boss`/`dungeon` by `RunState.room_kind()`. Tests: `music_core_test.gd` (resolve, crossfade endpoints/midpoint/clamp, required-ids guard, **every mapped file exists + loads + `loop==true`** — pins the .import state) + a save_data profile-migration test — **80 test cases green** (was 74; +5 music, +1 profile); smoke asserts `Music.current_id` per scene — **70 ok-checks green** (was 66). Known benign: same "N resources still in use at exit" shutdown notice as Sfx. **Next audio chunk: none — the first chunk is done;** the human curates real Suno/Udio tracks later. Human: music-map volume_db values are placeholder mix numbers — dial them like FEEL numbers.
- **StoryState autoload migration — story bookkeeping leaves game.gd (2026-07-04):** the "a StoryState autoload will own them later" note in game.gd is now cashed in — an internal refactor, **zero behavior change** (same counters, flags, save contents; smoke identical plus new asserts). Pieces: pure **`StoryCore`** (`src/core/story_core.gd`) with a function per EventBus-driven story mutation; thin **`StoryState` autoload** (`src/autoload/story_state.gd`, registered right after `SaveManager`) that subscribes to `resource_changed`/`death`/`boss_killed`/`run_ended`, delegates to StoryCore, and writes `SaveManager.state`. **Every mutation moved:** the `has-<resource>` first-pickup flags (resource_changed), the deaths counter (death), the boss_kills counter (boss_killed), and on run_ended the runs counter + `meta.runs` mirror + — on victory (== full clear in the slice) — full_clears + the codex shard + `codex_shard_added`. game.gd's `_on_run_ended` keeps only scene flow (checkpoint clear, day tick, tech auto-solve, deferred town swap + save); `_on_death`/`_on_boss_killed` and the pickup-flag lambda are gone. Cheat panel's raw pokes now route through StoryState (`set_flag`, `grant_codex_shard`) — same paths, deduped. **Ordering guarantee (verified):** autoloads `_ready` before the main scene, so StoryState connects to run_ended BEFORE game.gd, and game.gd's town-return save is deferred anyway — counters are always on disk first. **The smoke proves it** by re-reading the slot file after run 1 and asserting the persisted runs/full_clears/boss_kills/codex/meta.runs. **Design note:** StoryCore mutates the story dict IN PLACE (not TownCore/DialogueCore's return-a-new-dict — live call-site refs to the counters dict, e.g. the cheat panel's, must not go stale). Reads stay direct (town.gd day label, cheat panel readouts); DialogueCore's `mark_shown` contract is untouched. Tests: `story_core_test.gd` (11 cases — increment/pickup/codex/flag semantics + in-place proof). Validation: **99 test cases green** (was 88), smoke **93 ok-checks green** (was 88; +5 disk asserts), editor import clean. Docs synced: godot-conventions (autoload list + milestone), architecture-schemas (§1 implementation status). `TechState` remains the last target-only state autoload. (done 2026-07-05)
- **Placeholder audio — the SFX slice is live (2026-07-04):** audio.md's first-chunk step 2 built; **the combat-feel gate is no longer silent** (its verdict was supposed to postdate this). Pieces: **`default_bus_layout.tres`** (Master→Music/SFX/UI); pure **`SfxCore`** (`src/core/sfx_core.gd` — resolve id → {file, volume_db, pitch_scale, bus}, pitch jitter deterministic under test) under thin **`Sfx` autoload** (`src/autoload/sfx.gd` — 3D player pool for positioned combat sounds + 2D pool for UI, streams preloaded loudly at boot, PROCESS_MODE_ALWAYS so pause-panel UIs still click; **pickup + boss-kill ride EventBus**, gameplay never calls audio for bookkeeping); **`data/audio/sfx-map.json`** via `DataLoader.load_sfx_map()` (single-file map — a deliberate exception to one-file-per-entity: a mix wants one page; validated per-entry). **All phase-1 hooks wired:** combo hits ×3 + kill beside hitstop, dash, player-hurt, arrow loose/impact, exit-portal open, echo offer open/pick, day-tick chime, ui-click through the three panels' `_button` factories. **15 placeholder sounds are agent-synthesized** (deterministic script, jfxr register per audio.md; provenance table started in `assets/audio/SOURCES.md`) — Kenney packs still welcome, swapping a sound = editing its sfx-map row. Tests: `sfx_core_test.gd` (resolve math, feel-critical-ids guard, **every mapped file exists + loads**) — 74 test cases green + smoke green. Known benign: quitting with sounds mid-play prints "N resources still in use at exit" (AudioServer release order; documented in sfx.gd). **Next audio chunk: the `Music` autoload** (step 3 — placeholder tracks + crossfade) — **landed 2026-07-04, see the bullet above.** Human: sfx-map volumes/jitters are placeholder mix values — dial them like FEEL numbers.
- **gdUnit4 port — interim test runner retired (2026-07-03, unblocked by the human installing the plugin):** the human's editor action landed (AssetLib → **gdUnit4 6.2.0-rc2**, enabled in `project.godot`; the accompanying `feel_room.tscn` diff was pure 4.7 editor resave churn — uid annotations, **no FEEL values changed**, verified line-by-line) — committed as-is, which also **clears the audio first-chunk blocker** (audio.md's "uncommitted human edits" are in). The port kept every suite byte-identical: `tests/test_suite.gd` now extends `GdUnitTestSuite` and the `check`/`check_eq` helpers report through real gdUnit asserts with the interim runner's exact equality semantics (a throwaway canary suite verified failures actually fail: message + file:line, non-zero exit); `tests/test_runner.gd` deleted; native `assert_that(...)` equally fine in new tests. **Run: `GODOT_BIN=<godot> ./addons/gdUnit4/runtest.sh -a res://tests/core --ignoreHeadlessMode`** — 11/11 suites, **70/70 test cases green** (the old "N tests / M checks" metric becomes plain test-case counts; every check is an assert now); the editor's gdUnit panel works too; `reports/` gitignored. Smoke unchanged, green. Docs synced: godot-conventions (Testing section), audio.md + design/README (blocker cleared).
- **Name swap: the sister is Sophia, the second bearer is Linnea (2026-07-03, human decision, same day as the voice guides):** the historical mapping won — **Sophia Brahe was Tycho Brahe's actual sister and collaborator**, and the game's sister plays exactly that role, so she carries the name; the woman takes "Linnea" (no referent, arrives with Act II — the unnamed-in-Act-I rule is unchanged). **This was a code+data rename, not just docs:** `linneas-study` → **`sophias-study`** (building id + filename), dialogue speaker id `linnea` → `sophia` (b5), NPC/desk labels, hint text ("Sophia: …"), `knowledge.json` sources, town scene node `SophiasDesk`, tests + smoke. **Old dev saves that referenced `linneas-study` won't recognize the building** (acceptable pre-release; no migration written). All docs swapped in the same pass ("Sophia Brahe" historical references preserved). Validation: 70 tests / 295 checks + smoke 66 checks green post-rename.
- **Character voice guides locked (2026-07-03, worked out live with the human):** the last review gap closes — new spec **`design/voice-guides.md`**; batch snippet drafting is unblocked. **House style (human-decided):** plain timeless English; lines written to be SAID (short sentences, contractions); **banned AI constructions** — no em dashes in dialogue, no mirrored aphorisms, nothing that works as an inspirational post; jokes are spice, not sauce. **Per character:** stance + a syntactic tell (cover-the-name test) + human-approved sample lines as calibration anchors — Tycho (Zagreus-like but smarter, observation then quip), Sophia (serious, precise, corrects him), Thomas (Aquinas: the question under the question, "boy"), Tilly (report structure outrunning then fitting her ability, never witty), Mara (works while talking, banter), Herzog (Werner Herzog coded: short declaratives, no rhetorical questions, deadpan unexplained past; D3 refusal = "No."), Wren (mid-thought entry, license for modern vocabulary), the woman (telegram compression). **Naming decision (human; see the swap entry above):** the second bearer is named **Linnea** — `linnea` as data id, **never named in Act I**, the name arrives with Act II. **Tech-unlocked vocabulary table** (human-approved): town speech modernizes with research (no "minutes" before Mechanical Clock, no "zero" before Arithmetic, etc.; Wren + Linnea exempt) — a snippet-review checklist against gate conditions. Propagated: bible (Characters intro + the woman's entry + a new story-decision bullet), PRD §7.12, content-budget (§ Story & dialogue), design/README.
- **Act I endgame designed — Phase E (2026-07-03):** the post-climax dangle is closed: `act1-story-beats.md` gains **Phase E** (spine ~20 → **~22**). **E1 "A town that drills"** (force-play on next town return after D5): the town changes posture — drills, requisitions, manned cannon — and sets **`act1_complete`** (new save-meta field, schemas §1 → "Act I complete" slot badge); ends on the standing invitation (the portal still glows). **E2 "The artifact waits"** (gate: codex shards = max): the artifact assembles fully, turns, hums… and *listens* — complete ≠ solved, an explicit honest in-fiction "not yet" (IC-6: v1's payoff is the grown mystery); **written order-safe** (can fire before D5 for completionist rushers — no emperor references unless `d5`). **E3** (a state, not a scene): tiers (strata lore covers the reconfiguring), unfinished tech, remaining arc beats, and a **~10-bark post-climax set** (barks ~60 → **~70**) so the endgame town isn't frozen pre-war. Propagated: PRD (§6.3 win state, §7.11, §7.12 counts), content-budget (spine + barks rows), architecture-schemas (§1 meta). Scripts are skeleton-level like all beats — authored later.
- **Audio pipeline decided (2026-07-03):** the "budgeted but no pipeline" gap is closed — new spec **`design/audio.md`**. **SFX (~120):** CC0 packs (Kenney) + jfxr one-offs for placeholders AND most finals (layer/pitch in Audacity); `Sfx` autoload + **`data/audio/sfx-map.json`** (id → file/volume/pitch-jitter — swapping a sound is data); pitch jitter on combat sounds by default; feel-critical subset (combo hits ×3, kill, hurt, dash, arrow, pickup, echo-pick, door) human-tuned like `# FEEL:` numbers. **Music (~10):** AI-gen (Suno/Udio — human taste-curates from bulk generations) with FreePD/CC0 placeholders; .ogg loops; **per-stratum dungeon tracks** via the floor profile's existing `music_layer` (the budget's "3 intensity layers" moved post-v1); buses Master→Music/SFX/UI, volumes in profile settings; licensing rule (CC0/CC-BY only, `SOURCES.md` provenance from day one). **Key process point: don't judge the combat-feel gate silent** — placeholder hit/dash SFX land BEFORE the 20th-clear verdict. **First implementation chunk specced in the doc but deliberately NOT built:** it touches `project.godot` + combat scenes, which currently carry uncommitted human tuning edits — build it as its own chunk after those commit. Human actions needed: download 2–3 Kenney packs; pick Suno vs Udio; consider the dream-motif instrument (a musical tell for the woman, decided once, paid off in Act II). Propagated: bible (notes → "on audio"), PRD §12 assets, content-budget (music/SFX rows), design/README.
- **Food fixed — the Well-Fed upkeep mechanic (2026-07-03):** Food was a dead resource (its "soft cap on town size / morale" referenced a population system the lean town doesn't have). New spec **`design/food-upkeep.md`**: on each day tick the town **consumes Food** (base + 1/building); covered → **Well-Fed**: +25% (placeholder) to all other day-tick production **including Knowledge**; short → just no bonus — **never a penalty, no starvation state** (bonuses-not-punishments, like no-death-penalty). Surplus sells at the Market (existing). Rational-fiction dividend: agricultural surplus funds scholarship — a true thing as a game rule; gives Three-Field Rotation a felt payoff. **Act II prepare-for (the real point):** new town-tick effect kind **`upkeep`** (consume → status; schemas §6, tick order produce → upkeep → status-modified totals) generalizes to **army provisioning + per-city morale** — human troops eat, summons stay artefact-powered (IC-14 separation = a real strategic difference). Bible gains an **Upkeep role** row in the fixed-roles table. Propagated: bible (roles + resources + buildings tables), PRD (§7.10, §10 caps), architecture-schemas (§6), design/README. Design only; TownCore's `upkeep` kind lands with its consumer (built 2026-07-05).
- **Run structure — door choice + in-run healing designed (2026-07-03):** the meso loop's two missing specs, unified in **`design/run-structure.md`** ("damage costs *choices*, not progress"). **(1) Door choice:** after each clear, **two sigil-marked exit doors** preview the next room's reward — gold / ore / dust / **echo** / **reprieve-heal**, boss door alone at floor end; **peril marks** = elite-modifier room with boosted pay (the risk/reward lever + the elite system's first concrete use); no backtracking, never two same sigils, weights + pity in the floor profile (`data/floors/` gains `door_weights` + `peril_chance`; pity: ≥2 echo + ≥1 reprieve doors per floor); **Cartography's concrete effect = door foresight** (sigils one room ahead). **(2) Echo cadence DECIDED:** echo doors + one guaranteed pick after every floor boss, target **~12–17 picks/full run** — the slice's every-combat-room offer is a placeholder to retire when doors get built (code change deferred, flagged in run-structure.md + PRD §7.5). **(3) Healing:** attrition is the long game — **no full heals mid-run; all healing = % of *missing* HP** (anti-snowball vs Vitality): Wellspring behind Reprieve doors (~40%), automatic floor-boss repair (~30%), Recovery attunement (4/7/10%/clear), 2–3 healing echoes (Mender's Rhythm / Deep Repair / Salvage); explicit non-goals: no food drops, no mid-run shops. Watch-list: heal stacking (per-floor cap?), dust starvation, peril curve. Propagated: bible (Meso Loop), PRD (§7.5 trigger, §7.6, §9), content-budget (run-structure row: 6 sigils + peril mark + Wellspring prop), architecture-schemas (§9 floor profile), design/README. **Design only, nothing implemented** (built 2026-07-05 — see the status bullet above).
- **Etchings — the 9 abilities designed (2026-07-03):** the bible's empty etchings table (the last undesigned core combat system) is filled — new spec **`design/etchings.md`**. **Slot grammar:** RMB = **Strike** (quick, ~4–6 s CD), Q = **Field** (control, ~10–14 s), R = **Surge** (big moment, ~30–45 s); dash stays fixed on Space, outside the nine (upgraded only via Quickening + dash echoes). **Cooldowns only, no mana; mouse-aimed.** The nine: RMB **Push** (cone knockback, wall-slam/hazard shove — **granted free at B2**, the tutorial ability) / **Bolt** (ranged dart, staggers squishies) / **Afterstrike** (replays last melee hit); Q **Snare** (slow field) / **Ward** (temporary cover — blocks arrows + LoS) / **Lodestone** (groups enemies); R **Shockwave** (radial, the only armored-stagger) / **Surge** (~5 s overdrive) / **Sentinel** (short-lived construct — **the Act-II summon seed**, `source_etching`, `summon_seed: true`). Each: 3 levels (L3 = rider behavior) + exactly 1 weapon-synergy variant (3 per weapon); every ability = one real physical principle (`principle` field — deniable as magic pre-reveal, same rule as the strata). Dust costs placeholder (full kit ≈ 60–70 vs ~7 attunements — the deliberate two-sink tension). Unblocks: Echo "etching-mod" category now has handles. Balance watch-list: Ward vs ranged, Lodestone→Shockwave, rotation homogenization. Propagated: bible (table filled), PRD §7.3, content-budget, architecture-schemas (§10: `data/etchings/` shape), design/README. **Design only, nothing implemented; names + numbers await human review/playtest.**
- **Floor strata + hazards — design locked (2026-07-03, human decision):** IC-5's "no re-theme" reading is **revised to "strata, not biomes"** — the one dungeon's five floors now get individual identity from three cheap things on ONE shared geometry kit: a data-driven **environment profile** (palette/fog/light — `data/floors/`, "floor as data"), **one signature hazard** per floor (vent plates / watcher nodes / burst crystals / sweep beams / drift fields + shared denial mist — scripted timer+volume+telegraph, dual-use, `data/hazards/`), and 2–4 props. Independent biome art lines and age re-theming stay banned. **Lore engine:** the learning-space imitates Tycho's world and **the imitation thins with depth** (cave → borrowed hall → resonant crystal → filed geometry → the void core = the artifact chamber) — foreshadows the nanobot reveal visually; **deniability rule:** floors 1–4 must still read as "magic gets stranger". Hazards are diegetic test apparatus (runs are learning trials — each stratum probes one capability). New spec: **`design/dungeon-strata.md`** (strata table, hazard roster + rules, data shapes, readability guard: telegraph colors fixed across strata + 5 human legibility passes). Propagated: bible (Combat & presentation + Dungeons), PRD (§3, §5 IC-5, §7.6, §8, §11), content-budget (2 new rows), architecture-schemas (§9), design/README. **Nothing implemented yet** — design only; numbers are placeholders.
- **Design-doc review fixes (2026-07-03):** a design-docs review found a story-gate ordering bug + two doc drifts; all fixed. (1) **D4 gate bug:** the climax dream-glimpse (D4) was reachable via D3→D1→C3→C1 with only ONE full clear, while C4 (the *first* dream) needs `codex_shards >= 2` — a steady researcher could meet "the woman, clearer than ever" having never dreamt of her. D4 now also requires `flag(c4)` (`design/act1-story-beats.md`, note under the Phase D table). No code/data change — D4 isn't authored in `data/dialogue/` yet; the condition vocabulary already covers it. (2) **Shard-conversion drift:** bible said 1 shard = 1 knowledge; code deliberately uses 5 (`TechCore.SHARD_KNOWLEDGE_VALUE` — a shard must beat a passive day tick to stay the main early driver). Bible example updated to 5, marked a tuning placeholder. (3) **Godot-version drift:** bible + PRD still said 4.6 in present-tense spots; now 4.7 (dated history entries untouched). The review's remaining findings (etchings/hazards/healing/branching/Food/audio/voice-guides/v1-endgame gaps, self-graded gates, boss mini-gate) are feedback only — not yet triaged into the roadmap.
- **Dialogue system — first slice (2026-07-03):** the story-delivery engine is playable (PRD §7.12): pure **`DialogueCore`** (`src/dialogue/dialogue_core.gd`) implements the FULL act1-story-beats.md condition vocabulary (`flag` / counters incl. `codex_shards` / `tech` / `tech_started` / `age` / `resource` / `building` / `has` first-pickup / `talked_to`; AND-only per spec) + selection (each character offers their single best eligible snippet: source rank **spine > arc > contextual > bark**, author-weight tiebreak, id-asc determinism; `once`/seen; bark `cooldown_runs`; never two same-arc beats back-to-back) + `select_forced` + pure `mark_shown` bookkeeping (seen, `sets_flag`, cooldown stamps, talked_to). **`DialoguePanel`** (`src/dialogue/dialogue_panel.gd`): bottom talk box, E/click advances, cutscene kind dims the world (painterly stills later), fully scripted — no player choice (locked). **Town has 4 NPC talk spots** (Herzog/Mara/Tilly/Wren — Area3Ds with `metadata/npc_id`, E to talk) and **force-play on town entry** (max 1/visit — town `_ready` runs once per visit). **`data/dialogue/`** domain (spec in DataLoader) with 6 authored snippets: A4 fetch-framing (runs≥2), B1 Mara-ore (`has` resonance-ore), **B5 the-first-wall (force-play cutscene gated on masonry — pairs with the arch puzzle)**, a Herzog ledger contextual (gold≥50), C3 Tilly-reports (runs≥12 + clear — testable via the cheat panel's simulated runs), a repeatable Wren bark (cooldown 2). `has-<resource>` pickup flags are maintained from `resource_changed` in game.gd. Save `story` gained `talked_to`/`dialogue_last`/`arc_last` (defaults-merge; NOTE `mark_shown` REPLACES the story dict — don't hold references across it). Deferred: banner icon over speakers, stills, StoryState autoload migration. Validation: 70 tests / 295 checks; the smoke plays the beats (B5 force-plays on the town return after masonry research, flag+seen asserted; Herzog offers A4 over his contextual; talked_to counted) — 66 checks green.
- **F2 cheat panel (2026-07-03, human request):** playtest tool so the human can test puzzles, story progression, and economy **without grinding the dungeon**: `src/core/cheat_panel.gd`, spawned by `game.gd` on the HUD layer (survives scene swaps); **F2 toggles it** (pauses, like F1; inert on the slot-select screen). Offers: grant any resource +10/+100 (real Ledger adds, reason `"cheat"`) and +1 codex shard; **simulate 1/5 victorious runs or a death run — implemented as the exact EventBus signals a real run emits** (`boss_killed` → `run_ended`, plus the boss loot a real clear pays), so counters, the day tick, codex shards, Sophia's auto-solve, and every future subscriber (achievements, story gates) behave identically; ⚡ instant-research any available tech node (real `TechCore.complete` + `tech_researched`, so unlocks/age-advance cascade); in-run: slaughter the wave (real damage path — drops and the echo beat follow) and full heal. Sim buttons are town-only (a live run would go stale). Validation: 62 tests / 256 checks; the smoke drives the panel (grant, sim victory incl. counters/codex/day-tick asserts, codex-shard cheat) — 58 checks green.
- **Per-floor autosave checkpoints + slot-select screen (2026-07-02):** the two remaining §7.13 save promises are in. (1) **Checkpoints:** the save schema gained a `checkpoint` section (null when not mid-run; defaults-merge covers old saves) — as each floor's first room loads, `game.gd` snapshots `RunState.to_checkpoint()` (`{run, run_number, echoes, player_health}`) and saves; run end (win or die) clears it. Quitting mid-run therefore resumes at floor start via `RunState.resume_from` (JSON-roundtrip-safe int coercions; `echo_offers_made` rebuilt from the picks) — echoes still never outlive their run, the checkpoint just lets the same run survive a quit. (2) **Slot select:** F5 now boots to **`SlotSelect`** (`src/core/slot_select.gd`, code-built fullscreen page) — 3 slots with schema-§1 meta (name/age/runs/playtime/updated), a "⚔ mid-run — resumes at floor N" badge (`SaveManager.list_slots()` now reports `checkpoint_floor`), two-step delete confirm; `game.choose_slot()` loads-or-creates and routes to town or straight into the checkpointed run. Slot 1 is no longer implicit anywhere (the smoke's boot touches no slot at all). Validation: 62 tests / 256 checks; the smoke now runs 2 floors and **actually quits mid-run at floor 2 and resumes from disk** (echoes + carried HP asserted identical), 49 checks green.
- **Interactive arch puzzle — the content gate in its real form (2026-07-02):** the Masonry node's interim quiz is replaced by the authored build-the-gateway puzzle (`design/tech-nodes/medieval-masonry-the-arch.md` §5): pure **`ArchPuzzleCore`** (`src/learning/arch_puzzle_core.gd` — a scripted **3-beat state machine, deliberately NOT a physics sim** per §9: beat 1 the flat lintel cracks along its underside; beat 2 voussoirs need centering and the **keystone goes in last**; beat 3 the finished arch shoves **outward** and needs both feet braced — every failure is a pre-baked lesson event saying where and why) + a code-built clickable diagram **`ArchPuzzle`** (`src/learning/puzzle_arch.gd` — the gateway drawn in `_draw`, click slots to place stones (placeholder for §9's drag-and-drop), "Load it!" test button, round↔pointed profile toggle, Sophia's 3 authored hints from data, `solved` signal). **`TechPanel` now dispatches on `puzzle.kind`** (schema §4): `quiz` (arithmetic keeps it) | `interactive` (a `PUZZLES` scene registry; `solved` → the aha). `med-masonry-arch.json` puzzle = `{kind: interactive, scene: puzzle_arch, data: {intro, hints}}`. Deferred: real art/drag-drop, the 1-hint-per-run limit (hints just cycle), a mechanical effect for the pointed profile (visual-only thrust arrows now). Validation: 61 tests / 251 checks; the smoke drives the puzzle's full didactic path (lintel crack → unkeyed collapse → splay → the win). **The content-gate playtest can now happen in its intended interactive form at Sophia's desk.**
- **Weapons + Mara's Forge — milestone 3 checklist complete (2026-07-02):** the third vertical-slice system landed (PRD §7.2): **3 weapons as data** (`data/weapons/` — Sword/Daggers/Bow) applied to each room's fresh player by pure **`WeaponCore`** (`src/combat/weapon_core.gd`), then echoes on top. Design point: weapon defs are **relative add/mult mods over the player's feel-tuned exports** (same shape as echo mods) — the **Sword is the baseline with empty mods**, so the human's `# FEEL:` numbers and the F1 workflow stay the single source of combat feel; Daggers = fast/weak melee, **Bow = the first ranged weapon** (player.gd `weapon_kind`/`_handle_ranged_attacking`: draw → loose an arrow at facing → recover, no combo/lunge; `arrow.gd` generalized with speed/colour params + spawner-set mask so archer behaviour is untouched). **Flat upgrade track** (5 levels, +15% damage each, Resonance-Ore costs 1/2/3/5/8) bought at **Mara's Forge** in town (E; fullscreen `ForgePanel` — `src/town/forge_panel.gd` — equip + refine; resonance-effects track + Forge L2 later). **Resonance Ore now drops** (12%/kill + 2 guaranteed per boss). Save: `combat.current_weapon` added; `combat.weapons.{id}.flat` in use. Validation: 52 tests / 209 checks; smoke refines + equips daggers at the forge and asserts the daggers kit on the next run's player. **PRD §12 milestone-3 checklist is now functionally complete** (floor/boss/echoes/town/building/tech-node/save-load/weapons) — pending the human gates (feel 20th-clear call, content-gate playtest, asset gate).
- **Tech-node loop — the learning layer is playable (2026-07-02):** the game's soul-loop closed end-to-end (PRD §7.8): kill bosses → **Knowledge Shards** → **Sophia's Desk** in town (E) → pick a node → invest Knowledge (+shards convert at 5 apiece) → when funded, **read the authored explanation → quiz → "aha" reveal** → `tech_researched` fires → **typed unlocks apply** (the Quarry/Town Walls plots unlock live). Pieces: pure **`TechCore`** (`src/learning/tech_core.gd` — availability/prereqs, invest math, auto-solve counters; Sophia **auto-solves the active node after ~5 runs** once funded, ticked on run end — IC-10); **`TechPanel`** (`src/learning/tech_panel.gd`, code-built pause UI; wrong quiz answers just retry, never hard-gate); **tech data domain** (`data/tech/` — **`med-arithmetic-zero`** newly authored as a light support-node quiz, **`med-masonry-arch`** carrying the .md's full explanation/aha text with an **interim 3-question quiz standing in for the interactive arch puzzle** — the Content-gate build replaces it, the .md stays the authoring source); `data/buildings/town-walls.json`; **tech-gated build plots** (`TownCore.is_unlocked`; town.gd reworked to metadata-tagged multi-plots + the desk); age-advance hook on first tech of a new age (no-op at age 1). Save schema: `tech.active` added (defaults-merge covers old saves). Validation: 48 tests / 185 checks + the smoke now drives the whole research path (quarry blocked before, researched via the real panel incl. a deliberate wrong answer, buildable after, stone on the next day tick).
- **Echoes + in-run HP persistence (2026-07-02):** the pick-1-of-3 in-run upgrade beat is live (PRD §7.5). After every cleared **combat** room the game pauses and offers 3 Echo cards (click or 1/2/3; `src/combat/echo_offer_panel.gd`, code-built like the tuning panel). Pure, tested core: **`EchoCore`** (`src/combat/echo_core.gd`) — deterministic weighted offer generator (no dupes per offer; non-stackables leave the pool; **synergy echoes** unlock only when their `requires` are picked) + add/mult stat-mod math applied to the player's exported FEEL stats. **Content = data:** `data/echoes/` (8 starters incl. the `tempest-stride` synergy; spec in DataLoader). Picks live on **RunState** (in-run only, never saved — locked design) and re-apply to each room's fresh Player; picked list shows on the game HUD. Also: **player HP now carries between rooms** (`RunState.player_health`, saved at each exit portal, restored on spawn — rooms no longer free-heal), enabled by a small player refactor (`max_health` var over the `MAX_HEALTH` base const, `heal()`/`restore_health()`; raising max heals the delta, so Vital Core feels right mid-run). Validation: 41 tests / 158 checks + the smoke now plays the echo beat (picks a card each combat room, asserts picks + HP carry-over).
- **Vertical slice — run-loop spine built (2026-07-02, later the same day, on the human's "continue building"):** **F5 now boots the real game loop** (`scenes/core/game.tscn`), not the sandbox: town → step into the portal → a run of dungeon rooms → boss → back to town → day tick → save. Pieces: **`RunState` autoload** (thin) over pure **`RunFlow`** (`src/core/run_flow.gd` — run/floor/room progression, seeded per-floor room counts, last room of every floor = boss); **`combat_room.tscn`** (`src/combat/combat_room.gd` — ONE wave scaled by floor+room, gold drops via Ledger, clear → exit portal → next room; **F1 tuning panel works in-run too**); placeholder **boss** (`enemy_boss.tscn`, big armored purple Brute; drops bonus gold + `knowledge-shards`; `boss_killed` + counters wired via game.gd, StoryState will own them later); placeholder **town** (`scenes/town/town.tscn` — dungeon portal, **build plot**: Sophia's Study, 3 levels, walk in + E to build/upgrade, visibly grows); **`TownCore`** (`src/town/town_core.gd` — pure day tick + building helpers; 1 day = 1 run, fires win OR die; effect kinds `produce`/`knowledge` so far); **buildings data domain** (`data/buildings/quarry.json` + `sophias-study.json`, spec added to DataLoader) + `knowledge-shards` resource; codex shard on full clear; save on town return (slot 1 implicit — slot-select UI later); new `interact` (E) input action. **Slice run shape = 1 floor × 3–4 rooms** (`@export`s on `game.gd`; full game is 5×6–10, PRD §7.6). Validation: 34 tests / 104 checks green + an **end-to-end headless smoke** (`godot --headless res://tests/smoke/run_loop_smoke.tscn` — drives a full clear, a town build, and a death run on a throwaway slot; must run as a SCENE — `-s` scripts never get autoloads). The feel sandbox `feel_room.tscn` is untouched — open it and press **F6** for uninterrupted tuning. Not in this slice yet: weapons/loadout, unlock cascade, per-floor autosave checkpoints. (Echoes and the tech-node loop landed later the same day — see the bullets above.) (Echo offers/HUD landed later the same day — see the bullet above.)
- **Skeleton milestone — first slice built (2026-07-02):** the PRD §12 milestone-2 plumbing, started on the human's go-ahead while combat-feel tuning continues (feel gate playtest ongoing — early verdict "already feels good", 20th-clear bar not yet formally called). Built + registered autoloads: **`EventBus`** (`src/autoload/event_bus.gd`, the typed-signal spine — PRD §7.0), **`Ledger`** (thin wrapper emitting `resource_changed`; pure logic in `src/core/ledger_core.gd`), **`SaveManager`** (slots + profile at `user://saves/`, schema §1; pure defaults/migration-chain/defaults-merge in `src/core/save_data.gd`). **Data pipeline:** `src/core/data_loader.gd` + `src/core/validate.gd` (per-domain field specs, loud on errors, filename==id, typo-catching unknown-field checks) with first content in `data/resources/` (gold, knowledge, stone, resonance-ore) + `data/ages/1.json`. **Tests:** 24 tests / 67 checks green via an interim zero-dependency runner (`tests/test_runner.gd`; suites in `tests/core/`) — **gdUnit4 itself needs a human install** (editor → AssetLib → "gdUnit4"), agent sandbox can't integrate external plugin code; porting the suites is mechanical. Docs synced: `design/godot-conventions.md` (autoloads exist, interim testing), `design/architecture-schemas.md` (implementation status note).
- **Juice + readability pass & runtime tuning panel (2026-07-01):** (1) **Bugfix:** enemy body materials were shared per-variant scene (one Brute's telegraph turned *all* Brutes yellow) — now `resource_local_to_scene` in all enemy/player `.tscn`s. (2) **Hitstop** — whole-game freeze-frame on landed hits, scaled light < finisher < kill (`fx.gd` `hitstop*` static vars). (3) **Death FX** — shard burst + pop tinted per variant (`death_fx.tscn`/`.gd`, spawned from `enemy_dummy.gd`; `take_damage` now returns `died: bool` so the player can size hitstop). (4) **Post-hit grace** — `hit_grace` (0.45 s) i-frames after the player is hit, so crowds can't double-tap. (5) **Soft aim assist** — each swing lunges toward the nearest enemy in a forward cone (`player.gd` `lunge_*`, `_acquire_lunge`); whiffs still step forward; `lunge_speed = 0` disables. (6) **Hit stagger** — new enemy `stagger_time` export + `STAGGER` state: player hits interrupt squishies (Skirmisher 0.25 s, Archer 0.3 s) and cancel telegraphs; Brute = 0 = armored. (7) **Dash ghosts** — fading after-images during dash (`ghost_fx.gd`). (8) **Runtime tuning panel** — **F1 in game**: pauses, shows ~35 sliders (player/lunge/hitstop/camera/wave/crowd) applied live; "Copy changed" puts values in the clipboard to paste back into code. To support it, player/camera/feel_room FEEL consts became `@export var`s (snake_case, Inspector-tunable) and shared crowd rules + hitstop became `static var`s — all still `# FEEL:`-tagged; dial board updated (`design/feel-tuning.md`).
- **Upgraded to Godot 4.7 (2026-06-20).** `project.godot` `config/features` = 4.7; binary at `/home/clarior/Godot_v4.7-stable_linux.x86_64`. Project imports/compiles/runs clean. (4.6.3 binary still on disk; commands should use the 4.7 one.) **Godot MCP** (`@coding-solo/godot-mcp`) is wired up for this project in `~/.claude.json` with `GODOT_PATH` pointing at the 4.7 binary — `get_godot_version`/`get_project_info` etc. work.
- **Archer enemy + idle wander (2026-06-20):** third variant — a **ranged Archer** (`enemy_archer.gd` / `.tscn`, green, squishy): `extends EnemyDummy` and overrides only engage/strike — orbits at a stand-off, **kites** outward if the player gets inside `min_range`, and when in `shoot_range` with clear line of sight it telegraphs then **looses an arrow** (`arrow.gd`/`.tscn`, an `Area3D` projectile, mask = player+walls so no friendly fire, dies on any hit or after lifetime). Archers sit **outside the melee commit-token pool** (`is_committed()` → false) so they fire independently. The wave now rotates Brute/Skirmisher/Archer (`feel_room.gd` `_scene_for`, `i % 3`). Also: **idle enemies now wander** (stroll slowly between random nearby points, facing where they walk) instead of standing still, until they see the player — shared in the base `enemy_dummy.gd` (`_do_idle` + `_pick_wander`, `WANDER_*` consts).
- **Bigger feel room + cover + perception (2026-06-20):** `feel_room.tscn` floor enlarged 38→56 m (walls at ±27), with an `Obstacles` node of pillars + cover blocks (layer 4, so they block movement **and** line-of-sight rays). Enemies now start **dormant** (`enemy_dummy.gd` new `IDLE` state, rendered dimmer): they only wake to `ENGAGE` when the player is within `sight_range` (FEEL, 12 m) **and** in clear line of sight (raycast vs. layer 4 — cover blocks it); getting hit also wakes them; walk past `sight_range + SIGHT_LOSE_MARGIN` and they settle back to idle. The wave now **scatters around the room** (`feel_room.gd` `_wave_spawn_pos` ring, `SPAWN_RADIUS`/`SPAWN_JITTER`) instead of fanning in ahead, so the player must move in and some enemies start behind cover.

## Earlier status (2026-06-19)
- **Phase: Phase 0 — combat-feel gate (in human-playtest).** Design refined, decisions locked, **all four open story threads resolved**, 2.5D rendering confirmed, **pacing/economy/accessibility locked** (1 day = 1 run, 20–25 min runs, 5 floors × 6–10 rooms, no death penalty, no dungeon puzzle rooms, nanobot assist mode, combat-feel pass bar) — see `Tycho Roguelite.md` → "Story decisions — resolved", "Combat & presentation", "Pacing, economy & accessibility".
- **Git repo initialized** (branch `main`, Godot 4 `.gitignore` in place).
- **Godot 4.6 project created (2026-06-19)** to `design/godot-conventions.md` spec: `project.godot` (3D, GDScript, static typing, input map for WASD/dash/attack/RMB/Q/R), directory tree (`src/` by domain, `scenes/`, `data/`, `assets/`, `tests/`), reusable `scenes/core/camera_rig.tscn` (the one scene allowed to assume camera angles). Validated importing + compiling clean headless on Godot v4.6.3.
- **Combat-feel gate prototype built (2026-06-19)** — the Phase 0 go/no-go sandbox: `scenes/combat/feel_room.tscn` (main scene). One Tycho (`player.gd`: move + dash w/ i-frames + light-attack combo, faces mouse), a wave of real enemies (`enemy_dummy.gd`: orbit/strafe at a stand-off radius → close → telegraph → strike → recover → rest, stats `@export`ed so variants are data; two variants — Brute `enemy_dummy.tscn` + faster/weaker Skirmisher `enemy_skirmisher.tscn`; boids-style separation so they don't stack; a **commit-token cap** (`MAX_ATTACKERS`, counted across the `enemies` group — leak-free) so only N attack at once and crowds stay readable; `ENEMY_COUNT` mixed per wave, clear the wave to score a clear), placeholder primitive meshes, a clears counter + HP HUD. All feel numbers marked `# FEEL:`. **This builds the mechanism; the go/no-go is a human feel-tuning playtest** (game feel cannot be vibecoded) — pass bar: *after the 20th clear, you still want one more.*
- **Combat juice pass (2026-06-20):** camera shake on taking a hit (`camera_rig.gd` `shake()`), a swept blade visual + a translucent hitbox volume shown during the active frames so you can see what a swing covers (`player.gd` `_update_swing_visual`, nodes in `player.tscn`), and a bright slash streak left at each landed hit that fades over ~0.9 s (`slash_fx.tscn`/`.gd`). All cosmetic; all tunable via `# FEEL:` consts.
- **Combat depth pass (2026-06-20):** light attack is now a **3-hit combo** — hits 1 & 2 quick, hit 3 a finisher (more damage, wider arc, longer recovery); waiting longer than `COMBO_CONTINUE_WINDOW` resets the sequence (`player.gd`). **Floating damage numbers** on every hit (white normal / gold finisher / red on the player), via a small FX factory `CombatFX` (`src/combat/fx.gd`) spawning `damage_number.tscn` (billboard `Label3D`) and the slash. Enemy **separation** + variant system added (see enemies bullet above).
- **Asset-pipeline gate scene built (2026-06-20)** — Phase 0 gate 3 stage: `scenes/core/asset_pipeline_gate.tscn` (`character_stage.gd`). Loads a rigged/animated `.glb` (drop at `assets/models/gate_character.glb` or assign `model_scene`), finds its `AnimationPlayer`, force-loops + cycles clips under the **same fixed `camera_rig`**; A/D cycle clips, Space toggles a turntable. Runs a bobbing-capsule placeholder until a real model is dropped in. **Needs a human to produce/drop a model to actually validate the gate** — full step-by-step pipeline in `assets/README.md`.
- **Design-input docs drafted (2026-06-12):** v1 content budget, Act I story-beat skeleton (incl. dialogue-system spec), architecture schemas for the prepare-fors, Godot project conventions — all in `design/`. These are the inputs for the PRD.
- **PRD drafted (2026-06-19):** `design/prd.md` — the single implementable v1 spec, synthesized from the bible + design docs (systems with trigger/state/resolution, immutable core, MVP scope, balance risks, open questions). **Living doc, pre-development:** to be finalized after the Phase 0 gates pass (gate outcomes may revise counts/feel targets). It synthesizes, never overrides — the bible wins on conflict.
- **Passive Attunements layer added (2026-06-19):** persistent passive upgrades (max life, dash charges, regen, etc.) alongside the active etchings — bought with Resonance Dust at Thomas's Hut, distinct from in-run Echoes. See `Tycho Roguelite.md` → "Etchings" → "Passive Attunements"; budget in `design/content-budget.md`; save schema in `design/architecture-schemas.md`.
- **Next concrete action (HUMAN):** (1) playtest the combat-feel gate — **F5 now runs the full game loop** (town → portal → run); for the endless-wave sandbox open `scenes/combat/feel_room.tscn` and press **F6**, or `/home/clarior/Godot_v4.7-stable_linux.x86_64 --path . res://scenes/combat/feel_room.tscn`. **Press F1 in game** (works in the sandbox AND inside run rooms) for the live slider panel (pauses; tweak → F1 → feel; "Copy changed" → paste keepers into the scripts). **Press F2 for the cheat panel** (grant resources, simulate finished runs, insta-research) — no need to grind the dungeon to reach the puzzles or story beats. Full dial board with locations: `design/feel-tuning.md` (e.g. `src/combat/player.gd`, `src/combat/enemy_dummy.gd`, `src/core/camera_rig.gd`). Iterate until it's genuinely good (the 20th-clear bar). This is the project go/no-go. **Placeholder SFX are now live (2026-07-04)** — judge the bar with sound; dial any sound in `data/audio/sfx-map.json` (volume_db / pitch_jitter / file), no code. (2) In parallel, run the asset-pipeline gate (`scenes/core/asset_pipeline_gate.tscn`): produce a rigged+animated `.glb` per `assets/README.md` and drop it in. (3) ~~Install gdUnit4~~ — **done 2026-07-03** (suites ported, interim runner retired; see status). New (3): the audio pipeline's human inputs — download 2–3 **Kenney CC0 packs** (audio.md § SFX), pick **Suno vs Udio** for music, and mull the dream-motif instrument (the woman's musical tell). The Skeleton milestone's first slice (EventBus + Ledger + save/slots + data loader/validator — PRD §7.0, §12) is now built (see status); the vertical slice (PRD §12 milestone 3) is now underway — the run-loop spine is in (see status); the milestone-3 checklist is now functionally complete (weapons landed 2026-07-02), and the **interactive arch puzzle** now replaces the Masonry node's interim quiz (see status) — **the content-gate playtest can happen IN-GAME at Sophia's desk in its intended form** (research Arithmetic, then Masonry: read → build the gateway → aha; judge *delight vs. homework*). Per-floor autosave checkpoints + the slot-select boot screen landed too, and the **first dialogue-system slice is in** (see status) — talk to the town's four NPCs, and watch B5's wall cutscene force-play after researching Masonry. The **unlock-cascade pass is now in** (2026-07-04 — town facilities gate behind story flags b1/b3/b4; see status), and the **StoryState autoload migration is done** (2026-07-04 — story bookkeeping left game.gd for the StoryState autoload over pure StoryCore; see status), and the **TechState autoload migration is done** (2026-07-05 — the last target-only state autoload: tech-section bookkeeping left game.gd/panels for the TechState autoload over the existing pure TechCore; see status). The natural next agent chunks are more dialogue content (the contextual snippet pool — arcs + barks batch 1 landed 2026-07-04, see status; the stubbed `arc-sophia-telescope` awaits an Optics tech node) or the unlock-cascade's remaining polish. New (4): **curate the dialogue** — read `design/dialogue/drafts-review-2026-07-06.md` FIRST (your opening scripts integrated + the register recalibration + a conformance pass over the batch; the win-first-run edge was resolved 2026-07-07 via the a3-first-return twin — its §6 lists 5 placeholder lines to overwrite), then `design/dialogue/drafts-review-2026-07-04.md` (the 38-snippet batch 1). Edit lines freely — your edits become the new calibration anchors. (The placeholder-SFX pass landed 2026-07-04, and the `Music` autoload — audio.md step 3 — landed the same day, completing the audio first chunk; both synthesized-placeholder. See status.)

## Working agreement
- **Documentation is sacred.** Keep `Tycho Roguelite.md` (design bible) and this file accurate and current. A new agent must be able to pick up the project from these two docs alone. Update them as part of any task that changes scope, decisions, or structure.
- **Always update ALL affected md files in the same change — never just one.** Any change to scope, decisions, systems, or structure must propagate to every doc that touches it, so the docs never disagree. Before finishing such a task, check (and update where relevant): `CLAUDE.md` (status + working agreement), `Tycho Roguelite.md` (design bible — the source of truth), and the relevant `design/` files (`content-budget.md`, `act1-story-beats.md`, `architecture-schemas.md`, `godot-conventions.md`, `tech-nodes/`, `README.md`). Date new decisions and record them in the CLAUDE.md status. If docs conflict, the bible wins — fix the others to match.
- **Commit with git** after every meaningful change, with clear messages. (Repo not yet initialized — the first dev task initializes it.)
- **Push back** when something doesn't make sense — a workflow, an ordering, or a contradiction with the locked decisions. Don't silently comply.
- **Stack:** Godot 4.7, Linux, single-player.
- **Solo dev + AI:** the human owns ideas, story, playtesting, and combat-feel tuning; agents own systems, content, UI, and plumbing. **Game feel cannot be vibecoded** — leave feel-tuning to hands-on human iteration.
- **Assets:** working default is 2.5D (3D models on a fixed camera) — Tripo + Quaternius. Placeholder-first; validate look/feel before investing in final art.

## Build order (do in this order — earlier phases de-risk later ones)

### Phase 0 — Skeleton + the two gates (DO THIS FIRST)
1. Initialize a git repo and a Godot project (done — created on 4.6, since upgraded to 4.7).
2. **Combat-feel gate:** one throwaway room, one Tycho, one enemy, dash + light attack, placeholder models. Iterate ONLY on feel. **Pass bar: after the 20th clear of the same room, you still want one more.** It does NOT need to feel like Hades — its own feel is fine (Hades = quality reference, not cloning target). **This is the go/no-go for the entire project** — if a single room can't feel good, no story or tech tree will save it.
3. **Content gate:** author one complete tech-tree node end-to-end (explanation → puzzle → "aha"); playtest whether it reads as *delight* or *homework*. Fix the format before authoring the rest.
4. **Asset-pipeline gate** (validate, can run parallel to #2): ONE character Tripo → rig → Quaternius/UAL animation retarget → animated in a Godot scene under the fixed camera. De-risks the 2.5D pipeline assumption before any 3D asset work; failure ≠ no-go (fallback: stock Quaternius models, or learn Blender).

### Architectural "prepare-for" requirements (bake in from the start)
v1 builds only the roguelite, but must not paint us into a corner. Set these up early even though their full payoff is later:
- **Save system with multiple slots** — design the save schema before there's much to save.
- **Achievements** — a central event hook other systems fire into.
- **Age/era as data** — town, tech tree, and content keyed by age, so adding an age is data, not a rewrite.
- **Separable pillars** — keep roguelite / strategy / space concerns decoupled so Acts II (strategy) and III (space) can slot in later. **Do NOT build strategy or space gameplay in v1.**

### Phase 1+ — the v1 roguelite (detail TBD — do NOT pre-build)
Town hub, dialogue system, characters, tech tree, weapons, etchings, dungeon generation, enemies/bosses, meta-progression, resource economy. **Broken into real tasks only after the Phase 0 gates pass.** Too much is still open to plan in detail now — see the design bible's open authorial threads, plus second-order unknowns: tech-puzzle pacing between runs, resource-economy numbers, and how many weapons/etchings v1 needs.

## v1 finish line
End of Act I — the **evil emperor enters** as the antagonist (cliffhanger into the future strategy layer). NOT the alien reveal, NOT the war.

## Pointers
- Design bible / full vision: `Tycho Roguelite.md`
- Detailed design docs: `design/` (see `design/README.md`):
  - `design/prd.md` — **the v1 PRD** (start here to build; synthesizes everything below into an implementable spec)
  - `design/content-budget.md` — v1 scope by the numbers (the schedule)
  - `design/act1-story-beats.md` — story spine, character arcs, dialogue-system spec
  - `design/architecture-schemas.md` — draft schemas for the prepare-fors (save, ledger, ages, tech, achievements, town, dialogue)
  - `design/godot-conventions.md` — project structure, code rules, testing (read before writing any code)
  - `design/feel-tuning.md` — **dial board** for the combat-feel gate: every tunable `# FEEL:` number, what it does, and where to change it (Inspector `@export` vs. script `const`)
  - `design/dungeon-strata.md` — floor differentiation: the five strata, hazard roster + rules, the "imitation thins with depth" lore engine (locked 2026-07-03)
  - `design/etchings.md` — the 9 active abilities (+ fixed dash): slot grammar, levels, weapon synergies, Dust costs, the Sentinel summon seed (drafted 2026-07-03)
  - `design/run-structure.md` — door choice (sigils, peril, pity, Cartography foresight), echo cadence, in-run healing economy (drafted 2026-07-03)
  - `design/food-upkeep.md` — the Well-Fed mechanic: Food as town upkeep → production bonus; the Act-II provisioning prepare-for (drafted 2026-07-03)
  - `design/audio.md` — the audio pipeline: SFX/music production paths, sfx-map data, buses, licensing, first-chunk spec (drafted 2026-07-03)
  - `design/voice-guides.md` — how every character talks: house style, per-character tells + approved lines, tech-unlocked vocabulary; ALL dialogue drafting goes through it (locked 2026-07-03)
  - `design/tech-nodes/` — one file per tech node; first authored node (the Content gate): `medieval-masonry-the-arch.md`
- Asset resources: `Tycho Roguelite.md` → "notes" (Quaternius, Hotpot, Tripo, Blender)
